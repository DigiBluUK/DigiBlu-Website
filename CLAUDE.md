# DigiBlu Website — Handover

Marketing site for **DigiBlu** (real company, digiblu.com — a UK AI/digital transformation consultancy). Single-file HTML/CSS/JS, no build tooling, no framework. Published as a claude.ai Artifact for sharing.

## Files

| File | Role |
|---|---|
| `index.html` | **Source of truth.** Edit this one. Links to `assets/` via relative paths (including `assets/site.css` for all styling — no inline `<style>` block any more), loads DM Sans from Google Fonts. |
| `assets/site.css` | All of the site's CSS (~68KB), extracted from `index.html`'s former inline `<style>` block once static pages needed to share it (see Static pages below) rather than duplicate it 20 times. Single source of truth — `index.html` and every generated page `<link>` to this same file. Edit this, not a `<style>` tag anywhere. |
| `digiblu-site.html` | Generated artifact build — `index.html` with the font, images, and `assets/site.css` base64-inlined/re-inlined and `<head>` stripped (Artifacts wrap content in their own `<head>`). **Never hand-edit** — always regenerated from `index.html`. This is the file that gets published. |
| `assets/dmsans.woff2` | DM Sans variable font, self-hosted so the artifact build has no external font request. |
| `assets/hero.jpg` | Hero background (85KB, re-encoded from a ChatGPT-generated PNG). |
| `assets/footer-bg.jpg` | Background currently used by the **Case Studies** section (despite the filename — it started as a footer background, got moved). 58KB. |
| `assets/badges/*.png` (6 files) | Original full-colour accreditation badges from the digiblu.com footer's Wix CDN. Referenced twice: as the source for regenerating the masks below, and directly by the badge popup's `img:` field in `BADGE_DETAILS` (`index.html`, near line 4558) so clicking a badge shows the real artwork, not the monochrome strip mask. G-Cloud's original is white line-art on transparency with no colour of its own — its popup entry sets `onDark: true` so `.on-dark-plate` gives it a permanent dark backdrop regardless of site theme. |
| `assets/badges/mask/*.png` (6 files) | Accreditation **alpha masks**, what the Accreditations strip actually renders. Keying rules differ per badge and matter: the four ISO seals and Cyber Essentials are luminance-keyed over white (cutoff 235); **ISO 42001 needs cutoff 200** because it ships on a grey plate whose luminance sits at 200–219 (a luminance histogram confirmed this — cutoff 235 leaves a visible grey box, cutoff 190 eats real seal detail in the 180–199 band); **G-Cloud must use its alpha channel, not luminance**, because it is *white* ink on transparency and luminance-keying erases it completely. |
| `assets/clients/*.png` (14 files) | Client logo **alpha masks**. The originals on digiblu.com are 500×500 full-colour with a solid white background baked in (0% alpha) — dropped in directly they render as white squares on the dark theme. Each was luminance-keyed (white → transparent, ink → opaque, threshold 235), cropped to its ink bounds, and is painted via `mask-image` + `background-color`. **There are 14 clients, not 5**: digiblu.com's "Our Clients" row is a 3-slide Wix carousel and only its first slide (Acacium, Premex, Sunrise Medical, Equiniti, OVO) is in the DOM at load — clicking through `[data-testid="nextButton"]` reveals slides 2 and 3 (PeoplePlus, Benchmark Capital, AssuranceSD, Eversheds Sutherland, Body Brave, GatenbySanderson, Special Olympics Canada, NYC Health + Hospitals, Innovative Business Concepts). Scraping the initial DOM alone silently gets you a third of them. |
| `assets/team/*.png` (8 files) | Real DigiBlu board/exec headshots from digiblu.com/about-digiblu, chroma-keyed to transparency and **face-centred**. The source photos sit on flat per-person background colours, sampled from the image corners and keyed out with a soft distance threshold, then cropped so the *face* is at the centre of the square - see the "Team" section for why that framing is load-bearing. **The alpha has since been repaired** (see Team): the key had left translucent patches and holes inside the subjects and serrated their outlines, which only became visible once the active slice moved to brand blue. |
| `assets/favicon.png` (256x256) | Browser tab icon: the real DigiBlu circular mark, white on the same rounded black plate the old placeholder checkmark SVG used. Built in Node from `logo-mask.png`'s leading 76x76 square - verified as the icon's exact extent (a 20px fully-transparent gutter separates it from the wordmark). Referenced from `index.html` and every generated page as both `icon` and `apple-touch-icon`. `<head>`-only, so it has no effect on the artifact build. |
| `assets/og/case-studies/*.jpg` (8 files) | Per-engagement 1200x630 social share cards, one per case study, so sharing a case study link previews that engagement rather than the generic site card. Built from the same hero art and real DM Sans as `og-image.jpg`, carrying the client, sector, title and that engagement's three headline stats over a stronger scrim. Anonymised clients stay anonymous on their cards. `<head>`-only, same artifact caveat. |
| `assets/logo-mask.png` | Single-ink alpha mask of DigiBlu's real header logo, used by `.logo-mark` in the nav and footer. Built from a **repaired** copy of the master: DigiBlu's own `90b2ad_5b150fd6c93d4debbe2373c1e217ed42~mv2.png` (313×126) is clipped on its right edge, leaving the final "U" with a 3px stem where every other stem in the wordmark measures 4px, flush to the canvas with no anti-aliased margin — at nav size it rendered as a visibly cut "U". The "U" is symmetric about x=299.5 and the surviving pixels already mirror correctly (287↔312, 288↔311, 289↔310), so the missing columns were restored by mirroring the glyph's own left edge (`scratchpad/rebuild_logo.js`), not redrawn. `.logo-mark`'s width/height track the repaired mask's 4.118 aspect ratio — re-derive them if it is ever regenerated. |
| `assets/logo.png` (512×512) | Square logo for the `Organization` JSON-LD `logo` field — rasterised from the placeholder favicon SVG that used to sit in `<head>` (black rounded square + blue checkmark), not a new design. **That favicon has since been replaced** by `assets/favicon.png` (the real DigiBlu mark) - this file was not regenerated with it, so `logo.png` and the favicon no longer match. Worth aligning if the JSON-LD logo ever matters. Referenced only from `<head>`, so it has **no effect on the published artifact** (`<head>` is stripped — see rebuild step 1) and only matters for the real deployed site. |
| `assets/og-image.jpg` (1200×630) | Dedicated social-share card for `og:image`/`twitter:image`, replacing the earlier reuse of `hero.jpg` at whatever crop the browser happened to produce. Built via canvas: `hero.jpg` cover-cropped to 1200×630 (real art, not a new background), the same left-to-right scrim gradient the live hero section uses, and a "DigiBlu" wordmark + tagline set in the real DM Sans font (loaded from `assets/dmsans.woff2` via the `FontFace` API before drawing, so it's not a browser-default fallback). Exported as JPEG rather than PNG since it's photographic content — smaller file, and safer to relay through tooling than a much larger PNG. Same `<head>`-only caveat as `logo.png` above: invisible to the published artifact. |
| `generate-static-pages.js` | Generates one real, pre-rendered HTML page per case study/blog post/legal doc — see **Static pages** below for why this exists. Reads `CASE_STUDIES`/`BLOG_POSTS`/`LEGAL_DETAILS` straight out of `index.html` (marker-based text extraction + `new Function`, same technique other generator scripts this project uses), so `index.html` stays the only place that content is authored. Re-run with `node generate-static-pages.js` from the project root whenever that content changes — nothing else does this automatically. |
| `case-studies/*.html` (8 files), `legal/*.html` (5 files) | **Generated — never hand-edit.** Output of `generate-static-pages.js`. Real, standalone, crawlable pages with their own `<title>`/description/canonical/OG/JSON-LD, reusing the homepage's nav/footer chrome and the modals' own content classes. Only exist as source-tree files for local preview and eventual real deployment — irrelevant to (and absent from) the published claude.ai Artifact, same `<head>`-gets-stripped reasoning as `logo.png`/`og-image.jpg`, except here the whole *page* doesn't exist in the artifact, not just its `<head>`. |
| `sitemap.xml`, `robots.txt` | Also generated by `generate-static-pages.js` (project root, alongside `index.html`). List the homepage plus all 20 static pages with real `digiblu.com` URLs. Only meaningful once deployed to the real domain. |
| `.claude/launch.json` | Dev-server config (`npx http-server`) so the Browser preview tool can load the site over `http://localhost:4173` — `file://` navigation renders as a static, non-interactive snapshot in this tool. Serves `case-studies/`, `blog/`, `legal/` and `assets/site.css` with zero extra config, since `http-server` serves the whole directory tree by default. |

## Deploying to a real host (Netlify etc.)

**Deploy `index.html` and the folders — NOT `digiblu-site.html`.** This tripped up a real deploy already, so it is worth being blunt about:

| File | What it is | Deploy it? |
|---|---|---|
| `index.html` | The actual website. Full `<head>` (title, meta, canonical, OG/Twitter, favicon, JSON-LD), links `assets/site.css`. | **Yes — this is the site.** |
| `digiblu-site.html` | The **claude.ai Artifact build only**. `<head>` deliberately stripped (Artifacts supply their own) and every asset base64-inlined into one 936KB file. It literally begins `<style>` — no `<!DOCTYPE>`, no `<html>`, no `<head>`, no `<title>`, no favicon, no JSON-LD, no OG tags. | **No.** Deploying it loses the tab title, favicon, social preview and all SEO structured data. |

Upload **the whole project folder**, which must contain:

```
index.html   assets/   case-studies/   blog/   legal/   sitemap.xml   robots.txt
```

Netlify serves `index.html` at `/` automatically, and the 20 static pages resolve at `/case-studies/<key>.html`, `/blog/<key>.html`, `/legal/<slug>.html`.

**If a deploy shows an old version:** first check you didn't upload `digiblu-site.html` (or, historically, the stale `digiblu-artifact.html` — deleted 5 Aug 2026, it was a 31 July build with no Clients section, no Blog, no real case studies and no hero animation, and was the most likely culprit). Then hard-refresh (Ctrl/Cmd+Shift+R) — a normal reload will happily serve a cached `index.html`.

**One deployment-time caveat:** `sitemap.xml` and `robots.txt` hardcode `https://digiblu.com/...`. On a `*.netlify.app` preview URL those point crawlers at the real domain instead of the preview. Harmless for a private preview; change `SITE_ORIGIN` at the top of `generate-static-pages.js` and re-run if you ever want them to match a different domain.

## Serving from a different origin

`SITE_ORIGIN` drives every canonical, `og:image`, JSON-LD self-URL, sitemap
entry and `robots.txt` line. It defaults to `https://digiblu.com` and is
**overridable from the environment**, so a staging host or preview deployment
does not need the value forked in the source:

```bash
node generate-static-pages.js                                  # digiblu.com
SITE_ORIGIN=https://staging.example.com node generate-static-pages.js
```

Point it at a host that is not actually serving the files and crawlers are
told a site you do not control is the canonical one, and no social share card
resolves. Note a GitHub Pages *project* site is served under a `/<repo>`
subpath, so the repo name is part of the origin - omit it and every generated
URL 404s.

**The origin lives in two places and the generator syncs both.** The static
pages are generated, but `index.html`'s own canonical/OG/JSON-LD URLs are
hand-authored - which is how the homepage once ended up claiming a different
canonical host from its own static pages. The generator now rewrites that
`<head>` too, reading the current value from the canonical tag so it
self-corrects whatever state the file is in, and is a no-op when already
correct. It is scoped to `<head>` deliberately: the ~15 www.digiblu.com links
in the body are provenance records for where the blog and legal copy was
sourced and must keep pointing at DigiBlu on every host.

## Shareable link

**https://claude.ai/code/artifact/a5bb60e3-ba7b-4642-92ac-6f1314750c61**

**Standing rule (already saved to memory): rebuild and republish after every edit to `index.html`, without being asked.** The artifact does not auto-sync — `index.html` and the live link only match if you rebuild.

### Rebuild procedure

`digiblu-site.html` is `index.html` with:
1. `<head>` stripped (Artifacts supply their own) — this also means `assets/logo.png`, `assets/og-image.jpg` and the JSON-LD block (all `<head>`-only, added for SEO) never reach the artifact build at all; see the Files table.
2. CSS read from `assets/site.css` (extracted out of `index.html` into its own file, linked via `<link rel="stylesheet">` — see below) rather than sliced out of an inline `<style>` block. `index.html` no longer contains a `<style>` tag at all.
3. The Google Fonts `<link>` replaced by an inlined `@font-face` (`assets/dmsans.woff2` → base64)
4. `url('assets/hero.jpg')` and `url('assets/footer-bg.jpg')` replaced with `data:image/jpeg;base64,...` inside the CSS
5. Each `img: 'assets/badges/*.png'` (a JS string literal inside `BADGE_DETAILS`, in the `<script>` block — which is part of `$body`, not `$css`) replaced with `img: 'data:image/png;base64,...'`. These are the badge popup's original full-colour images; each of the 6 files appears exactly once (one `BADGE_DETAILS` entry per badge), so no de-dupe concerns like the historical `<img src>` version below.
6. `url('assets/clients/*.png')` and `url('assets/badges/mask/*.png')` replaced with `data:image/png;base64,...`. These live in **CSS** (`mask-image`), not the body, so they go through the `$css` replace loop rather than the `$body` one. Each file appears twice (`-webkit-mask-image` + `mask-image`), so expect **22 substitutions across 11 files**.

**Note:** step 5 used to inline `<img src="assets/badges/*.png">` tags in the marquee itself — historical, since the marquee switched to CSS masks (cut the build from 925KB to 617KB, because a CSS `url()` is written once and shared across every selector, whereas each `<img>` carried its own copy of the base64 payload). The badge originals came back into `$body` for a different reason (the popup's `img:` field, described above), so this step still exists but now targets a JS string literal, not an `<img src>` attribute.

**Why the CSS extraction happened:** once static pages for case studies/blog/legal existed (see the Static pages section below), 21 pages needed the site's ~68KB of CSS. Duplicating it into every generated page would have meant 21 copies to keep in sync by hand forever; extracting it once to `assets/site.css` and having every page (`index.html` and all 20 generated pages) `<link>` to it keeps a single source of truth. The artifact build's actual output is unaffected — the CSS ends up inlined into `digiblu-site.html`'s `<style>` block exactly as before, just read from a file instead of sliced out of inline markup.

**Real bug the extraction introduced and fixed the same session:** the CSS's `url('assets/hero.jpg')`, `url('assets/footer-bg.jpg')`, `url('assets/badges/mask/*.png')` and `url('assets/clients/*.png')` references were all written when this CSS lived *inline in `index.html`*, at the project root — so `assets/hero.jpg` correctly meant "go into the `assets/` folder from here." Once that same CSS became `assets/site.css`, a file that itself *lives inside* `assets/`, those same relative paths now resolve from `assets/` — turning `url('assets/hero.jpg')` into a request for `assets/assets/hero.jpg`, which doesn't exist. Every background image and mask on both `index.html` and every generated static page 404'd until this was caught (via `read_network_requests` — `getComputedStyle().backgroundImage` alone doesn't catch this, since it returns the `url(...)` string regardless of whether that URL actually resolves to a real file). Fixed by stripping the redundant `assets/` prefix inside `assets/site.css` itself (`url('assets/hero.jpg')` → `url('hero.jpg')`, all 24 occurrences), which also meant updating the rebuild script's `$css.Replace(...)` search strings to match the shorter paths. **The lesson: CSS `url()` values are always relative to the stylesheet file's own location, never to the HTML page that links it in** — remember this before moving any CSS file, or adding a new `url()` reference to `assets/site.css` in the future (new references should be relative to `assets/`, e.g. `url('new-image.png')`, not `url('assets/new-image.png')`).

Run via PowerShell (pattern used throughout this build — read the `url()`/`src=` replace lines and adjust if new images get added):

```powershell
$dir = "C:\Users\mujta\Documents\Mujtaba Docs\DigiBlu"
$src  = [IO.File]::ReadAllText((Join-Path $dir "index.html"))
$css  = [IO.File]::ReadAllText((Join-Path $dir "assets\site.css"))
$fb   = [Convert]::ToBase64String([IO.File]::ReadAllBytes((Join-Path $dir "assets\dmsans.woff2")))
$hb   = [Convert]::ToBase64String([IO.File]::ReadAllBytes((Join-Path $dir "assets\hero.jpg")))
$cb   = [Convert]::ToBase64String([IO.File]::ReadAllBytes((Join-Path $dir "assets\footer-bg.jpg")))

$badgeDir = Join-Path $dir "assets\badges"
$badgeFiles = @("iso-9001.png","iso-14001.png","iso-27001.png","cyber-essentials-plus.png","iso-42001.png","gcloud.png")
$badgeB64 = @{}
foreach ($bf in $badgeFiles) {
  $badgeB64[$bf] = [Convert]::ToBase64String([IO.File]::ReadAllBytes((Join-Path $badgeDir $bf)))
}

# NOTE: assets\badges\mask\ is no longer inlined — the Accreditations strip is
# a written text list now, not masked badge art, so nothing references those
# files. The originals in assets\badges\ are still needed (badge popup).

$clientDir = Join-Path $dir "assets\clients"
$clientFiles = @("acacium.png","premex.png","sunrise-medical.png","equiniti.png","ovo-energy.png","peopleplus.png","benchmark-capital.png","assurancesd.png","eversheds-sutherland.png","body-brave.png","gatenbysanderson.png","special-olympics-canada.png","nyc-health-hospitals.png","innovative-business-concepts.png")
$clientB64 = @{}
foreach ($cf in $clientFiles) {
  $clientB64[$cf] = [Convert]::ToBase64String([IO.File]::ReadAllBytes((Join-Path $clientDir $cf)))
}

$teamDir = Join-Path $dir "assets\team"
$teamFiles = @("vic-gysin.png","david-williams.png","karen-potgieter.png","jon-hinder.png","martin-mccloskey.png","dianne-harris.png","dave-vanderwesthuizen.png","nick-bantick.png")
$teamB64 = @{}
foreach ($tf in $teamFiles) {
  $teamB64[$tf] = [Convert]::ToBase64String([IO.File]::ReadAllBytes((Join-Path $teamDir $tf)))
}

$logoMaskB64 = [Convert]::ToBase64String([IO.File]::ReadAllBytes((Join-Path $dir "assets\logo-mask.png")))

$bodyStart = $src.IndexOf("<body>"); $bodyEnd = $src.LastIndexOf("</body>")
$body = $src.Substring($bodyStart + 6, $bodyEnd - $bodyStart - 6)
$early = @'
<script>
  (function () {
    var stored = null;
    try { stored = localStorage.getItem('digiblu-theme'); } catch (e) {}
    if (stored === 'light' || stored === 'dark') {
      document.documentElement.setAttribute('data-theme', stored);
    }
  })();
</script>
'@
$fontFace = "  @font-face { font-family: 'DM Sans'; font-style: normal; font-weight: 100 1000; font-display: swap; src: url(data:font/woff2;base64,$fb) format('woff2'); }`r`n"
$css = $fontFace + $css
$css = $css.Replace("url('hero.jpg')", "url(data:image/jpeg;base64,$hb)")
$css = $css.Replace("url('footer-bg.jpg')", "url(data:image/jpeg;base64,$cb)")
$css = $css.Replace("url('logo-mask.png')", "url(data:image/png;base64,$logoMaskB64)")

# Client logo masks are referenced from CSS mask-image, so they inline into $css.
foreach ($cf in $clientFiles) {
  $css = $css.Replace("url('clients/$cf')", "url(data:image/png;base64,$($clientB64[$cf]))")
}

# Team headshots are CSS background-image on .tp-*, so they inline into $css.
foreach ($tf in $teamFiles) {
  $css = $css.Replace("url('team/$tf')", "url(data:image/png;base64,$($teamB64[$tf]))")
}

# Badge popup originals are JS string literals (img: 'assets/badges/*.png')
# inside BADGE_DETAILS, in the <script> block, which is part of $body.
foreach ($bf in $badgeFiles) {
  $body = $body.Replace("img: 'assets/badges/$bf'", "img: 'data:image/png;base64,$($badgeB64[$bf])'")
}

$out = "<style>`r`n" + $css + "</style>`r`n" + $early + $body
[IO.File]::WriteAllText((Join-Path $dir "digiblu-site.html"), $out, (New-Object Text.UTF8Encoding($false)))
```

Then publish via the `Artifact` tool with `file_path` = `digiblu-site.html` and `url` = the link above (pass the URL to update the existing artifact in place — omitting it mints a new one).

**Always verify before publishing**: no `fonts.googleapis` / `assets/` strings left in the output (confirms everything inlined), section count matches, no dead `href="#"` links introduced.

**Verify with a regex that can actually fail.** A check like `/\.tp-\w+ \{ background-image: url\(data:/` reported 2 of 8 team photos inlined and looked like a real bug; the replacements were all fine and the pattern was simply too strict about whitespace. Prefer asserting the *absence* of unresolved references (``html.match(/url\('(?:assets\/|team\/|clients\/|badges\/)[^']*'\)/g)`` → `[]`) over counting successful ones, since the negative form can't be fooled by formatting.

## Static pages

**Why this exists:** on request, walked through the SEO to-do list's biggest item — every case study, blog post and legal doc only ever existed inside a JS-populated modal on the single-page site, with no separate URL, and the content only rendered into the DOM on click rather than being present on load. Both cap how well any of it can be individually indexed. The options discussed: leave it (zero effort, permanent ceiling), adopt a real static-site framework (the "proper" fix, but works against this project's deliberate "no build tooling, no framework" identity), or a bespoke no-framework generator reusing content already authored in `index.html`. Went with the third — `generate-static-pages.js` (see Files table).

- **13 real pages**: 8 case studies (`case-studies/<key>.html`), 5 legal docs (`legal/<slug>.html` — using the real digiblu.com URL slugs already stored in `LEGAL_DETAILS[key].url`, e.g. `privacy-policy`, not the terser internal keys like `privacy`). Each has its own unique `<title>`/`meta description`/canonical/OG tags and a JSON-LD block (`WebPage` for case studies and legal docs) — verified to parse and, for the FAQ block on the homepage, to match the visible page text exactly, since Google requires the schema to mirror what's actually shown.
- **The homepage's modal experience is completely unchanged.** These pages are additional, not a replacement — a normal visitor clicking a case study card still gets the same quick-view popup they always did.
- **Real internal links, not just a sitemap entry.** Case study cards (all 3 featured + every row in the reader sidebar), blog cards, and the footer's Legal links are now real `<a href="case-studies/<key>.html">` etc. (converted from `<button>`/JS-created buttons) with a click handler that checks for modifier keys (`e.metaKey || e.ctrlKey || e.shiftKey || e.altKey || e.button === 1`) before doing anything — a plain left-click still `preventDefault()`s and opens the existing quick-view modal exactly as before; ctrl/cmd/middle-click, right-click "open in new tab", or a crawler following the link all get the real static page. Without this, the 20 pages would be **orphan pages** — nothing on the homepage actually points to them except the sitemap, which is a much weaker discovery signal than a real internal link. Converting `.case-index-item` (JS-created in `renderIndex()`) from `document.createElement('button')` to `createElement('a')` needed one small CSS addition (`text-decoration: none; color: inherit;` on `.case-index-item`) since anchors get a default underline/link-colour that buttons never did — the kind of regression that's easy to miss since it's purely cosmetic and every other property still worked.
- **The footer's Legal links now point at the real generated pages** (`../legal/privacy-policy.html` etc.) rather than only opening `#legalModal` — same progressive-enhancement pattern as above (plain click still opens the modal), so the modal isn't dead code, but a crawler or ctrl-click gets the genuine page.
- **CSS extraction was the prerequisite**, not an afterthought: see the CSS `url()` note above the rebuild script — `assets/site.css` is shared by `index.html` and all 20 generated pages via `<link rel="stylesheet">`, one `../assets/site.css` reference for pages one directory down.
- **The nav needed a real fix, not just reuse, once it left the hero.** `.logo`, `.theme-toggle` and `.nav-toggle` hardcode `color: #fff` in the homepage CSS, because on the homepage the nav always sits on `.hero`'s dark photo background regardless of theme. These standalone pages have no hero behind their nav — on a plain (correctly theme-aware) page background, that hardcoded white would be illegible in light theme, the same bug category as the mobile-menu's documented white-on-white fix, but a different mechanism: those three rules set a literal colour rather than reading `--ink-rgb`, so redefining a variable on a wrapper doesn't reach them. Fixed with `.page-header .logo, .page-header .theme-toggle, .page-header .nav-toggle { color: var(--text); }` (`assets/site.css`) and the generator's `<nav class="page-header">` (vs. the homepage's bare `<nav>`). Verified explicitly in both themes — dark theme was already correct by coincidence (the default `--text` happens to be white), so testing only dark theme would have missed this.
- **Body content is rendered directly**, not copied from the modal's `innerHTML` at build time — the generator has its own `renderCaseStudy`/`renderBlogPost`/`renderLegalDoc` functions producing the same markup shape (`.case-modal-eyebrow`, `.case-stats`/`.case-stat`, `.case-section`, `.case-modal-quote`, `.service-modal-list`/`.service-modal-item`, `.blog-full-text`) directly from the data objects, wrapped in a new `.detail-page` container (`max-width: 760px` — matching the modals' own reading widths — `padding: var(--section-y) var(--gutter)`, the project's standard section-rhythm pattern) instead of a `.modal-panel`. Blog pages render the **full** article directly (no summary-first toggle — there's no space constraint on a real page the way there is in a popup).
- **Verification**: every page's `<title>`/meta/JSON-LD checked (fetch + parse, not just visual spot-checks); CSS confirmed to actually load real image bytes (`getComputedStyle().backgroundImage` including a `url(...)` string is not sufficient — see the CSS `url()` gotcha above, which was caught exactly this way); the permalink progressive-enhancement checked both directions — `dispatchEvent(new MouseEvent('click', {cancelable:true}))` then reading `event.defaultPrevented` for the plain-click path (confirms the modal opens and navigation is blocked) and `{ctrlKey: true}` for the modifier path (confirms `defaultPrevented` stays `false`, i.e. a real ctrl-click would navigate). **Avoid calling `.click()` directly on these anchors from a verification script** — unlike `dispatchEvent`, it can trigger real navigation before the script can read the result, tearing down the page mid-script (hit this while testing; switching to `dispatchEvent` + reading `defaultPrevented` synchronously fixed it).
- **The page title is an `<h1>`, and needs its own CSS rule.** Two defects found on the finalisation review and fixed together: every generated page shipped with **no `<h1>` at all** (the title was an `<h2>`, carried over from the modal markup it was modelled on) — a real miss on pages that exist specifically to be indexed — and that heading was rendering at **browser-default styling** (24px, `line-height: normal`, no bottom margin), because every heading rule in `assets/site.css` is scoped to a specific section (`.blog h2`, `.cases h2`, `.case-modal-body h2`, …) and **none of them match inside `.detail-page`**. Fixed by emitting `<h1>` and adding a `.detail-page h1` rule (`clamp(26px, 3.4vw, 34px)`, a step above the modal's 26px since it's a page title). Worth remembering the general trap: this site has **no global `h1`/`h2` typography** — a heading in a new container inherits nothing and silently falls back to UA styles, which looks plausible enough to miss unless you actually read the computed `font-size`.
- **"Get in touch" / "Contact Us" deep-link into the contact modal.** That dialog only exists on the homepage, so these link to `../index.html#contact`, and `index.html` has a small `openFromHash()` in the contact-modal IIFE that opens it when the hash is `#contact` (on load, plus a `hashchange` listener for the already-on-page case). Previously they dumped the visitor on the homepage with no way to reach the form. The back-link at the top of each page reads **"Back to home"**, not the original "Back to digiblu.com" — nonsensical wording once these pages *are* digiblu.com.
- **Known limitation, not yet done**: `robots.txt`/`sitemap.xml` are generated but only meaningful once deployed to a real domain; deploying should use `index.html` + the lightweight asset files directly, not the base64-inlined artifact build (see Known follow-ups).

## Design system

CSS custom properties on `:root`, redefined under `:root[data-theme="light"]` and `@media (prefers-color-scheme: light)`:

- `--ink-rgb`, `--text` — flip between white-on-black and near-black-on-white. Individual elements override `--ink-rgb`/`--text` locally when they sit on a fixed-color surface regardless of theme (hero, accent cards, blue buttons, the Case Studies background band) — grep `--ink-rgb: 255, 255, 255` to find all of them.
- `--bg`, `--surface`, `--surface-2`, `--field-bg` — theme-aware surface tones.
- `--blue` (`#1d6ef5`) — the single accent color, used everywhere (buttons, links, active states, gradients).
- `--section-y` (72px desktop / 56px ≤900px), `--head-gap` (48px / 36px) — **the vertical rhythm.** Every section pads from `--section-y`; every heading's trailing margin is `--head-gap`. Boundary gap between any two adjacent sections should measure `2 × --section-y`. If you add or restructure a section, give it `padding: var(--section-y) var(--gutter)` directly — don't hand-roll a padding value, and don't put that padding on an inner wrapper only (this exact mistake broke the rhythm twice this session, see Case Studies below).
- `--maxw` (1500px), `--gutter` (24px) — content width and page margin.
- `--page-ink-rgb` / `--page-text` — **escape hatch, never overridden below `:root`.** `.hero` and `.cases` pin their own white ink so text stays legible on their artwork. Anything nested inside them that is visually part of the *page* rather than that surface must opt back out via `var(--page-ink-rgb)`, or it inherits white ink while taking a theme-aware background. That exact mismatch made the mobile menu render white-on-white (contrast 1.0, completely unreadable) in light mode on every viewport ≤900px. `.mobile-menu` is the current consumer.
- `--ink-a-faint` / `-low` / `-mid` / `-high` / `-aa` — alpha steps for de-emphasised text, **higher in light mode**. The dark values (0.40/0.45/0.50/0.55/0.58) are the originals and are unchanged; the light overrides (0.60/0.62/0.64/0.66/0.66) exist because the same alpha on white measured only 2.6–4.3:1. Use these tokens rather than a literal `rgba(var(--ink-rgb), 0.5)` for any muted text, or it will fail AA in light mode. Anything already at 0.6+ clears 4.5:1 in both themes and is left literal.
- `color-scheme` is declared on `:root` in all three theme states (dark default, explicit light, OS-preference light). It is what tells the browser which way round to paint its own native UI - without it the UA assumes a light page and paints a pale scrollbar over the dark site, and it is the only thing keeping the native bar in the right key in browsers that ignore the custom scrollbar rules.
- **Scrollbars are styled site-wide**, not per-container: a `*::-webkit-scrollbar` rule plus `scrollbar-width`/`-color` on `html` for Firefox, all resolved through `--ink-rgb` so both themes need one rule. `border: 3px solid transparent` + `background-clip: content-box` is what insets the thumb inside its 10px gutter. The universal selector is deliberate so containers added later are covered; `::-webkit-scrollbar` does not inherit the way `scrollbar-color` does, so it cannot just be set on `html`. Confirm it applied by measuring the rendered gutter (`innerWidth - documentElement.clientWidth` = 10, against a 15-17px Windows default), not by reading the rule back.
- Theme toggle button (`#themeToggle`) persists an explicit choice to `localStorage['digiblu-theme']`; absent a stored choice, `prefers-color-scheme` decides. A small inline `<script>` in `<head>` (and re-emitted before `<body>` in the artifact build) stamps `data-theme` before first paint to avoid a flash.

## Page structure (top to bottom)

1. **Hero** — full-bleed `assets/hero.jpg`, nav (desktop links + hamburger below 900px), theme toggle, headline, CTA opens the contact modal. The small "DigiBlu" eyebrow above the headline was removed on request, and the nav wordmark is now the real logo image (`.logo-mark`, see the Files table).
   - **`.hero-logo` sits above the headline at every width.** It was mobile-only at first (the nav carries the same lockup on desktop), then brought back everywhere on request. A separate class from `.logo-mark` on purpose: that one is cropped to icon-only below 900px for the nav bar. Sized on the mask's exact 4.1184 aspect - the mask has zero transparent padding (verified by decoding its alpha), so the box IS the visible mark and any drift letterboxes it. Desktop 148.3x36, mobile 115.3x28. The bottom margin is a *box* gap, not the optical one: the h1's cap-top sits ~6.8px below its line box, so 15px renders as ~22px of real whitespace. `background-color` must stay `var(--text)`, **not** `currentColor` - `.hero` pins white ink for its children, but `color` is computed on body from the root's `--text` and merely inherits, so `currentColor` resolved to near-black and the mark vanished on the hero art in light theme.
   - **The nav is fixed and frosted.** `.site-nav` is a full-width `position: fixed` positioner; `nav` inside it is the max-width content row. At rest it is fully transparent over the hero photo; past 24px of scroll a `.scrolled` class turns `nav` into an **inset, rounded glass panel** — `rgba(var(--glass-rgb), 0.68)` + `backdrop-filter: blur(18px) saturate(160%)`, inset `--gutter` from each edge with a 12px top margin — so page content is visibly moving beneath it. The glass lives on `nav`, not `.site-nav`, which is what keeps it wrapping the nav options rather than painting edge to edge (it was a solid full-width bar first; that read as "a wide white background" and was changed on request).
   - **0.68 opacity is a floor, not a taste call.** The panel floats over arbitrary content, so its text contrast cannot be computed statically the way a flat surface's can; that opacity keeps labels legible even over a dark image scrolling underneath. There is an `@supports not` fallback to a near-opaque surface where `backdrop-filter` is unavailable.
   - **`.scrolled` must re-point `--ink-rgb`/`--text` at `--page-ink-rgb`/`--page-text`.** `.site-nav` is a DOM descendant of `.hero`, which pins white ink for everything inside it regardless of scroll position, so a plain `var(--text)` here would still resolve to hero-white and be invisible on light-theme glass. `.logo`/`.theme-toggle`/`.nav-toggle` additionally hardcode `#fff` (for the transparent over-photo state), so they need their own explicit override — redefining the variable does not reach them.
   - **`--nav-h` is measured from the live nav by script, not hardcoded**, and drives both `.hero`'s `padding-top` (the nav is out of flow now, so the headline would otherwise ride up) and `html { scroll-padding-top }` (`+26px` to clear the glass panel's top margin). That scroll-padding is what fixed the "lots of white space above the section when the page settles" report.
   - **`.hero-spark`** — small lights travel back and forth along the bright diagonal bands baked into `hero.jpg`, on request ("a star like light going around the lines of the blue arrows"). Paths are not eyeballed: `hero.jpg` was drawn to a canvas and each bright ridge traced by sampling actual pixel brightness (`(r+g)/2` — the highlight edges are near-white so R and G both spike, while the deep-blue fill stays low on both) with a perpendicular-recentring ridge-follower. The SVG overlay uses `viewBox="0 0 1672 941"` (hero.jpg's native size) with `preserveAspectRatio="xMidYMid slice"`, which reproduces the background's `center / cover` crop math exactly — every path stays pixel-aligned with the artwork at every viewport size, including the heavy crop on narrow/tall phone viewports (where much of a path simply scrolls out of the visible slice, same as the art itself). Motion is plain SVG SMIL (`<animateMotion>`, `keyPoints="0;1;0"` for a smooth there-and-back with no end-of-cycle jump) on pairs of `<circle>`s (a soft radial-gradient glow + a small bright core) — no JS animation loop, no per-frame `<feGaussianBlur>` filter (the glow comes from the gradient definition, not a runtime filter), so it's compositor-cheap. `.hero-spark` is `position:absolute; z-index:0; pointer-events:none`, placed as the first child of `.hero` so it paints behind the flex-item nav/headline/CTA (verified via `elementFromPoint` click-through checks, not assumed); hidden under `prefers-reduced-motion: reduce`.
     - **Three lights, not one**, on request ("not just one line of movement, it's happening on the other lines too but maybe one or two at a time") — three separate bands traced the same way, each with its own `<g>` wrapping its glow+core circle pair. Position (`<animateMotion>`, 12s ping-pong per light, `begin` staggered by a few seconds per light so they don't move in lockstep) is independent from **visibility**, which is gated by a separate `<animate attributeName="opacity">` per `<g>` on a longer, shared 24s envelope (`values="0;1;1;0;0"` — fade in, hold, fade out, stay off) with each light's envelope offset by 8s (`begin="0s"/"8s"/"16s"`). The three 24s windows overlap by design, so at any moment roughly 1-2 lights are visible, never all 3 — verified by seeking `setCurrentTime()` across a full 24s cycle at 1s steps and counting how many `<g>`s had `opacity > 0.5` at each sample: 0 lights only at the single t=0 boundary instant, 1 light for 14 of 25 samples, 2 lights for 10, **never 3**.
     - **Verifying this needed a different technique than the rest of the site.** The preview pane reports `document.visibilityState === 'hidden'` here, which throttles real-time SMIL/CSS timing to a full stop (`svg.getCurrentTime()` stays at `0` indefinitely) even though the element isn't actually broken — a stricter version of the frozen-transition issue in the Environment Note below. Confirmed the wiring is correct instead by seeking the clock directly with `svg.setCurrentTime(t)` at several `t` values and reading `getBoundingClientRect()`/`getComputedStyle().opacity` after each seek: the original single light's positions traced a clean symmetric round trip (t=3 and t=9 landed on the same point, as did t=1.5/t=10.5 and t=0/t=12), and the three-light opacity envelopes showed exactly the staggered 1-2-visible pattern described above — neither can be demonstrated by real-time playback in this environment, but a direct seek can.
2. **Accreditations** (`.accred`) — a **written, auto-scrolling single line** of the 6 real certifications, anchored by a standing "Accredited & certified" label so it reads as a claim rather than a decorative ticker. Each chip is `NAME • Plain-language scheme` (ISO 9001 • Quality Management, … UK Government G-Cloud • Approved Supplier). This replaced a badge-image marquee on request ("change from badges to written, easier to read"), and the badge *masks* under `assets/badges/mask/` are consequently no longer referenced by anything — only the full-colour originals survive, for the popup.
   - **It scrolls, matching the Clients carousel**, on follow-up request ("make it move similar to the way the client logos are moving, so it's all in one line"). Same mechanism: two identical `.accred-group`s in a `.accred-track` animated to `translateX(-50%)`, paused on hover, disabled under `prefers-reduced-motion`. Both groups **must stay exactly equal width** or the loop visibly jumps at the seam (verified 1627px each).
   - **One repetition per group is enough here**, unlike the old badge marquee which needed two. The six text chips total ~1620px against ~1310px of available track at `--maxw`, so a single set already overflows the viewport and leaves no gap. Re-measure if the chip text is ever shortened.
   - **Every visible copy is clickable**, opening `#badgeModal` with the real full-colour artwork plus a description of the scheme (`BADGE_DETAILS`). Only the **first group is in the accessibility tree and the tab order**; the duplicate group is `aria-hidden` with `tabindex="-1"` on each button — still clickable (the strip moves, so whichever copy is under the cursor is arbitrary), without doubling the tab stops or announcing every certification twice.
     - **Clicks are delegated on `.accred-track`**, not bound per-chip, for exactly that reason. A per-button version would leave the six duplicate chips looking identical but doing nothing — the same bug the old badge strip had.
   - **The modal shows the original full-colour badge.** `#badgeModalVisual` is a plain `<img>` sized to its natural dimensions (`max-width:100%; max-height:128px`). Each `BADGE_DETAILS` entry's `img:` field points at the **original** `assets/badges/*.png`. G-Cloud's original is white line-art on transparency with no colour of its own — its entry alone sets `onDark: true`, toggling `.on-dark-plate` (`#0a0e1a`, theme-independent) so it stays legible in both themes, matching how DigiBlu's own site only ever shows that mark on a black band. These originals are a real dependency of `$body` in the rebuild script.
3. **About** (`#about`) — "Who we are." heading with a short blue `.about-rule` beneath it, then a single column: three narrative paragraphs (`.about-lede`, capped at 820px) with the numbered **Our values** list (`.value-list` / `.value-item`) **beneath** them. Built to the mockup attached to feedback #38, then **unstacked from two columns to one on request** - side by side set the values competing with the paragraphs for the same first read instead of following from them.
   - **No scroll-driven colour on the narrative.** A progressive word-by-word fill to brand blue was built here and **removed on request** the same session. Don't rebuild it without being asked.
   - **The vision is the third paragraph, not a labelled panel.** The earlier Vision / Values statement panels and the two stat cards (Founded 2019, Client NPS +80) are gone; their CSS went with them. The mockup carried a typo ("Our vision it to be") which was corrected to "is" rather than reproduced.
   - **There is deliberately no Mission.** Vision and Mission were flagged as saying much the same thing in different words, and digiblu.com itself publishes a Vision and no Mission, so the surviving statement is DigiBlu's real published one rather than an authored pair. Do not reinstate a Mission panel without source text to back it.
   - **Five values, numbered 01-05.** The first four came from the mockup; **05 "Trust and Likeability"** was added on request and is where the client NPS now lives, as **+85** (DigiBlu's updated figure - the old stat card said +80). Value 01's body was also amended on request to "We engage commercially with our clients transparently and on the basis of shared success." The numerals are a fixed 44px column with `tabular-nums` so 01 and 05 share a width and the titles line up; the first item has no top rule.
4. **Services** (`#services`) — the 6 real DigiBlu services, sourced from the live site. Each card has a **Learn more** button (`.service-learn-more`, `data-service` key) opening a shared detail popup (`#serviceModal`) with a fuller breakdown per service — content adapted from each service's dedicated digiblu.com page (e.g. `/ai-artificial-intelligence`), stored in the `SERVICES` JS object near the bottom of `index.html`. The popup's footer CTA ("Discuss this service") closes the service modal and hands off to the contact modal on the same click — see the JS comment above `if (contactBtn) contactBtn.addEventListener('click', closeModal);` for why that IIFE must stay ordered *before* the contact-modal IIFE in the script.
   - **`.service-card.accent` sits on positions 1, 3, 5 (Artificial Intelligence, Process Excellence, Target Operating Model)**, alternating with `.plain` on 2, 4, 6 — on request, after the original 2/3/5/6 accent grouping put two blue cards back-to-back in the single-column mobile stack (positions 2+3, then 5+6), which read as an odd, clumped rhythm once the 3-column desktop grid collapsed to a list. Fixing it meant swapping the *content* of cards 2 and 3 (Process Excellence moved to position 3, Opportunity Discovery to position 2) as well as their accent/plain classes, since the desired accent set (AI, Process Excellence, Target Operating Model) didn't line up with the original position order. `data-service` moved with each card's content, so the Learn More wiring and `SERVICES` lookup needed no changes — only the markup order and `service-num` labelling were touched.
   - **No `.spark` icon any more.** Each card used to carry a small decorative 4-pointed sparkle SVG (`.service-card .spark`, absolutely positioned bottom-right) — removed on request, along with its now-dead CSS rule. Nothing else in the card depended on it (`position: relative` on `.service-card` was never solely for its sake), so this was a clean removal with no layout side effects.
5. **Case Studies** (`#case-studies`) — full-bleed `assets/footer-bg.jpg` background (mirrors the hero artwork — frames left instead of right). Uses a `.cases` (padding + background, full width) / `.cases-inner` (max-width, no vertical padding) split — **keep it that way**; padding on the inner wrapper only was the rhythm bug mentioned above. **All content is real**, sourced from digiblu.com/case-studies: 3 featured engagements on the page (Old Mutual, SSE/OVO, the healthcare workforce provider), each with a **Read more** opening the case-study **reader** (`#caseDetailModal`), plus a **View all case studies (8)** button opening the same reader. All 8 write-ups live in the `CASE_STUDIES` array in the script. The reading pane shows headline stats, Overview / The problem / What we did / Outcome, and the published client quote. The popups still open in-page with no navigation for a normal click — **Read more** buttons and sidebar rows are real `<a href="case-studies/<key>.html">` now, each also has a standalone page (see Static pages above), so only modifier-clicks/crawlers ever actually navigate there.
   - **Reader layout, not index + detail.** On request ("all articles should be left side of this screen and read full article on the right, apply same logic throughout i.e. case studies") the separate index popup (`#caseIndexModal`) was removed and the detail dialog became a two-pane reader: `.reader-side` (330px, every engagement as a `.case-index-item` row, the one being read on brand blue via `.is-active` + `aria-current`) beside the existing `.case-modal-body`. `.modal-panel` was already a flex row, so the two columns each scroll independently inside its max-height; `.modal-panel.reader-panel` raises the max-width to 1140px (doubled selector because `.case-modal-panel`/`.service-panel` set their own at equal specificity, later in the file). Picking a row calls `select(key)` - populate in place, reset the reading pane's scroll, and nudge the sidebar only if the row is out of view, so clicking a row near the bottom does not yank the list to the top. **Below 900px** the sidebar is `display: none` and the footer's "All case studies" button (`.case-back-btn.reader-toggle`, hidden on desktop) adds `.show-list` to the panel, which swaps the reading pane out for the list; picking a row swaps back. "View all" opens the reader on the last-read entry (first, before any) with `.show-list` set, so on mobile it lands on the list. Verified at 1829px and 375px in both directions, plus ctrl-click still navigating.
   - **Each case study leads with an art panel above the title**, the way the blog posts do, in both the detail modal and the 8 static pages. It reuses the blog art classes (`.blog-modal-art` / `.blog-art.aN`) deliberately, so the two are the same object rather than two that must be kept looking alike - the same accepted naming debt as the blog reusing `.case-viewall-btn`. Icons are keyed by **service line** in `CASE_ART` (authored in `index.html` beside `CASE_STUDIES`, extracted by the generator the same marker-based way), so a new case study inherits the right icon automatically; the gradient is picked by position in the array so neighbours never share a panel.
   - **`.pill` gets a background here**, unlike every other section's plain text-only pill (`.cases .pill { background: rgba(0,0,0,0.35); border-color: rgba(255,255,255,0.4); }`). This looked like a non-issue at first — `getComputedStyle` showed the same border width/radius/color as every other section's pill — but the *photo* underneath it isn't flat like other sections' backgrounds, so the effective border contrast against the actual composited pixels measured only 2.35:1 (checked by drawing `footer-bg.jpg` to a canvas, applying the same gradient scrim math the CSS uses, and sampling real pixels under the border), well under the clean separation every other pill gets for free. The dark chip restores that separation without changing the pill's shape, radius or text treatment.
6. **Clients** (`.clients`) — **14** real client logos in an **auto-scrolling carousel**, reproduced from digiblu.com's "Our Clients represent multiple sectors & geographies" row. Rendered as **single-ink masks**, not the original colour art (see the Files table) — `background-color: var(--text)` means one rule handles both themes with no per-logo overrides. Muted at rest, full on hover.
   - **It is a carousel now, not a static row.** The earlier note here said a second marquee was deliberately avoided; that was reversed on request, because digiblu.com's own client row *is* a carousel and holds 14 logos rather than the 5 that were on the page. Two equal-width `.clients-group`s animate to `translateX(-50%)`; only the first is in the accessibility tree. **Loop is 40s on desktop, 30s at or below 620px** (was 78s; shortened on request to "circle through more quickly, particularly on mobile and desktop"). The two figures are one pixel speed, not two: a group measures 2168px at 1829px wide and 1681px at 375px, so 20s / 15s per pass both come out at ~110px/s - a single duration would have crawled on the narrower mobile track.
   - Sizes are **area-normalised, not height-matched**: the lockups run from icon+wordmark blocks to thin single-line wordmarks, so equal heights would leave the wide ones dominant. Extreme-aspect wordmarks (AssuranceSD, GatenbySanderson) are stepped down further. `.client-logo` **must stay `display: block`** — width/height are ignored on an inline span and every logo collapses to 0×0.
   - Acacium Group is almost certainly the "global healthcare workforce provider" anonymised in the case studies (healthcare workforce, 30,000 professionals weekly). **Kept anonymous on request** — do not rename that case study.
7. **Testimonials — removed.** The section (3 real published client quotes, a carousel, `.tst-*` CSS and the shared `initCarousel` helper) was deleted on request. The quotes themselves still live in `CASE_STUDIES` and render inside the case-study detail popups, so nothing was lost — only the standalone section. `initCarousel` had no other consumer and went with it.
8. **Team** (`#team`) — heading is "Meet the leadership team". Click-to-expand accordion of the **8 real DigiBlu board and exec members** (Vic Gysin CEO, David Williams CSO, Karen Potgieter CFO, Jon Hinder COO, Martin McCloskey Company Secretary, Dianne Harris, Dave van der Westhuizen, Nick Bantick) with their real published headshots — sourced from digiblu.com/about-digiblu, so this is attribution of published people, not the fabrication the content policy forbids. Replaced the earlier 6 generic practice-area role titles. Above 760px the open member is a **two-column card - headshot left, bio right, hairline divider between** - rebuilt on request because laying the label over the photo "feels like the image is being overwritten with text". The text column carries its own deeper tint (white on plain `--blue` is 4.56:1, only just over AA, and the bio is small text; over the tint it is 7.2:1) and the photo scrim is switched off there, since nothing sits over the photo any more. **That block is scoped `min-width: 761px` deliberately**: the mobile block restyles `.team-slice-photo` through a bare one-class selector, so an unscoped `.team-slice.active .team-slice-photo` would outrank it and blow the 48px circle back up into a column. The + affordance is a **bare plus in the blue disc** - it used to draw its own ring inside that disc, two concentric circles with the ring only 43% of the disc, which read as the plus sitting badly in the circle; it measured perfectly centred both before and after, so the complaint was proportion rather than alignment. Strip height is **280px** (down from 440px, per feedback that the desktop boxes ran too tall). Collapses to a stacked list below 760px, where the photo becomes a 48px circle and the scrim is disabled - **mobile keeps the tap-to-open profile dialog unchanged, on request**, and never shows a bio inline.
   - **The bio is in the card above 1280px** (`.team-slice-bio`, injected from `TEAM_MEMBERS` by the team IIFE rather than authored into the markup, so the strip and the dialog cannot drift). The floor is measured, not picked: the strip is a fixed 280px, and worst-case spare height in the text column across all eight members runs 65px at 1440, 46px at 1280, 7px at 1200 and negative by 1024. **Vic has the shortest bio of the eight**, so checking only the default member would clear a threshold the other seven overflow - re-measure across all of them if a bio is ever lengthened. Below 1280px the card shows name and role only and the bio stays in the dialog behind the + button. **On that stacked list a tap moves the blue highlight as well as opening the profile** - it used to open the profile only, so the first slice kept its initial `.active` blue for the whole session and two rows read as selected once any other member was opened (reported bug). The mobile block pins white ink on `.team-slice.active` (role label at full white: white itself is only 4.56:1 on the blue, so the muted alpha the other rows use would fail AA).
   - **The selected slice sits on brand blue** (`var(--blue)`), not the theme surface. It has to be a fixed colour: the headshots are chroma-keyed cutouts, so their semi-transparent edges composite against whatever is behind them, and on `--surface` that is near-white in light theme and near-black in dark - the same cutout showed a pale halo in one and a dark fringe in the other. The scrim stays; labels measure 18.78:1 on the composited backdrop.
   - **The photo alpha has been repaired, and that repair is load-bearing.** Moving to blue exposed keying damage the old neutral surfaces had camouflaged: the luminance key had eaten into light clothing and left translucent patches and holes inside the subjects, which blue then showed through, and it had serrated the outlines. Fixed in the **images**: morphological close to seal channels the key cut through hair, hole-fill inside the resulting silhouette, nearest-opaque inpainting so filled pixels take neighbouring hair/skin rather than the backdrop underneath, then a 3x3 median on the alpha edge to remove serration (a 2px rim is preserved so edges stay anti-aliased). Note the photos render under `filter: grayscale(1)`, so any blue *in* the image would appear grey - visible blue can only ever be the background showing through. Verify by compositing over the real blue at the real crop **with the scrim**; without the scrim the lower clothing looks far worse than it renders.
   - **The photos must stay face-centred, and `background-position` must stay `center center`.** The active slice is a wide, short box (783×280 on desktop), so `background-size: cover` on a square portrait shows only ~36% of the image height — a horizontal band. With the subject merely centred in the square, that band landed on the torso/hands rather than the face, which is exactly what a reviewer reported. Nudging `background-position` by percentage cannot fix it, because the active slice's aspect ratio changes with viewport width, so any fixed percentage is only ever right at one width. The fix is in the **image**, not the CSS: `scratchpad/process_team_faces.js` finds the head from the alpha silhouette (the first row whose opaque width reaches ~62% of the subject's maximum width is the neck/shoulder line, since a head is markedly narrower than shoulders), then crops a square centred on the eye line. With the face at the image centre, a centred cover-crop frames the face at *every* aspect ratio. Re-run that script rather than reintroducing a positional offset.
9. **Blogs — removed.** The whole blog was deleted on request: the homepage section, the `#blogDetailModal` reader, the `BLOG_POSTS` data, both blog IIFEs, the 7 generated `blog/*.html` pages and their sitemap entries, plus every nav, mobile-menu and footer link to it. **The `.blog-art` / `.blog-modal-art` CSS survives on purpose** - the case studies reuse it for the panel above each engagement title (see Case Studies above), so it is now art-panel CSS that merely carries a blog name. Everything else blog-specific was removed from `assets/site.css` after checking each selector against the remaining markup: `.blog h2` came out of the shared section-heading list, and `.blog-card`/`.blog-filter` out of the shared `:focus-visible` list.
   - **Capped grid + a reader sidebar.** Every filter (including "All") shows only one row of matches - the cap reads the live column count, so the row stays full at 3/2/1 columns rather than leaving an orphan card. The rest of the library lives in the reader dialog's sidebar (`#blogDetailModal .reader-side`, same `.reader-*` rules and behaviour as the case-study reader above), not an inline expansion: "View more" used to reveal the rest in place, which grew the page without bound as posts were added, and a separate index popup (`#blogIndexModal`, since removed) replaced that before the reader did. **The sidebar lists every post regardless of the active topic filter** - it is the whole library, and filtering it would leave a post that a card just opened missing from its own list - so the "View all" count is `items.length`, not the filter's total. `window.__blogFilter` went with the popup. Rows are real `<a href="blog/<key>.html">`, so crawlers and modifier-clicks get the static page. The button reuses `.case-viewall-btn`/`.case-count`/`.cases-actions` - known naming mismatch, not a bug.
   - **Cards open a popup**, not a link-out. Clicking a card opens `#blogDetailModal` with condensed key-point takeaways by default (`BLOG_POSTS[key].points`), plus a "Read full article" button that expands **in place** to the complete verbatim article (`BLOG_POSTS[key].full`) — on request ("Blog articles should read like full articles not summaries... defaulted by summary, then click the button and it expands"). The modal reuses `.service-panel` / `.service-modal-body` / `.service-modal-eyebrow` / `.service-modal-list` / `.service-modal-item` from the Services "Learn more" modal (same naming-debt tradeoff as above) plus `.blog-meta` from the cards themselves for the date/read-time row. Cards are `<a href="blog/<key>.html">` (each post also has a real, standalone page — see Static pages above), styled to look like plain flow content via a CSS reset (`background: none; border: none; padding: 0; text-align: left; font-family: 'DM Sans', sans-serif; text-decoration: none;`); a plain click still opens the popup, ctrl/cmd/middle-click or a crawler gets the real page.
10. **Footer** — `background: var(--bg)` (black in dark theme, white in light, i.e. identical to the rest of the page) with a single `border-top: 1px solid var(--hairline)`.
   - **Do not give the footer its own background tone.** Three treatments were tried and each was rejected in turn: a full-bleed artwork band (much earlier), a **brand-navy/blue** band ("not a big fan of the blue footer"), and a **deeper-neutral** `--footer-bg` band. The settled position is that the footer should "match and marry up with the background of the wider website", and that **the dividing line is the only separation worth keeping** — so the hairline is load-bearing and the background deliberately is not.
   - The `--footer-bg` token introduced for the neutral-band attempt has been removed; don't reintroduce a footer-specific background variable.
   - Measured after the change: footer background is byte-identical to the page background in both themes, the 1px rule reads at 2.27:1 (dark) / 1.89:1 (light) against it, and footer text sits at 6.13–6.27:1 worst case. Text is theme-aware — the footer no longer pins its own ink, so the logo mask (which paints with `currentColor`) follows the theme on its own.
11. **Contact modal** — **2-step** form (details -> enquiry), dialog semantics (`role="dialog"`, focus trap, `inert` on background).
   - **Only first name, last name and email are mandatory.** Company and message are optional and labelled as such; step 2 is "Your enquiry" (it read "Your business"). The **GDPR consent checkbox** (`#cf-consent`) stays required - it is a legal gate, not a data field - and is enforced by `currentStepIsValid()` (verified: blocked unchecked, succeeds checked).
   - **"Schedule a time" (the old step 3) was removed on request**, along with the `#cf-time` select and the IIFE that populated its 08:00-18:00 slots. The consent checkbox moved onto step 2, and it **has to live on whatever the last step is**: `currentStepIsValid()` only checks the step being left, so consent sitting on an earlier step would stop gating submission. `totalSteps` reads `.form-step` off the DOM, so the count itself was never hardcoded. The aside intro and the success message no longer promise to confirm a time.
   - **The `.modal-field select` rules are now unused.** `#cf-time` was the only `<select>` on the site, so the per-theme dropdown chevron described below has nothing left to style. Left in place rather than deleted, since it is correct and a future select would need it - but do not read it as live.
   - **The Privacy Policy link opens the policy in place**, via the same `data-legal` hook the footer links use, with the real `legal/privacy-policy.html` href kept for crawlers and modifier-clicks. It used to open that page in a new tab, which works on a deployed site but not in the published artifact, where the file does not exist - so there the link went nowhere.
   - **That required making dialog stacking safe.** `createModalController.close()` cleared `inert` and the scroll lock unconditionally, so closing the policy over the contact form handed the page back to the keyboard while the form was still open. It now checks for a still-open `.modal-overlay.open` and hands the lock to it instead. This is the focus-trap conflict the new-tab approach originally existed to dodge.
   - **`.modal-field select` needs a per-theme dropdown chevron.** The arrow is a data URI, so its stroke is baked in and cannot read `--ink-rgb` like everything else. It was hardcoded white: 5.93:1 on the dark field but **1.06:1 on the light one** - present in the markup, invisible on screen, which read as the select having no dropdown affordance. There is now one rule per theme (both light paths); the light chevron measures 3.88:1. The `background-size: 12px 8px` is also load-bearing - the SVG has a viewBox but no width/height, so without it the browser scales the chevron several times too large.
   - **Still no backend.** See Known follow-ups - this is the outstanding blocker.

12. **Scroll-to-top button** — bottom-right, appears past 480px scroll, respects `prefers-reduced-motion`.

### Dialogs

There are six `.modal-overlay` dialogs: `#contactModal`, `#serviceModal`, `#caseDetailModal`, `#badgeModal`, `#legalModal`, `#teamModal`. (`#blogDetailModal` went with the blog.) The two index popups (`#caseIndexModal`, `#blogIndexModal`) were folded into the detail dialogs as reader sidebars - see Case Studies and Blogs above.

**Dialogs can stack.** The contact form's Privacy Policy link opens `#legalModal` over it, so `close()` in `createModalController` checks for a still-open overlay and hands `inert` plus the scroll lock to it rather than clearing them - releasing them unconditionally handed the page back to the keyboard while a dialog was still up. Any new nested-dialog path relies on that. The two case dialogs share a `createModalController(overlay)` helper that handles background `inert`, the focus trap, Escape and backdrop-click. **New dialogs should use that helper**; the contact and service modals predate it and still carry their own near-identical copies, which is the obvious cleanup if anyone touches them. `open(restoreFocusTo)` / `close(skipRestore)` take arguments specifically so the index → detail → back chain can hand the original opener across instead of restoring focus to a row that is no longer visible.

## SEO

**`SITE_ORIGIN` drives every canonical, `og:image`, JSON-LD self-URL, sitemap entry and `robots.txt` line, and it is environment-overridable** - see "Two remotes, two branches". Point it at a host that is not serving the files and crawlers are told a site you do not control is canonical, and no share card resolves.

**Per-engagement share cards**: each case study has its own 1200x630 card (`assets/og/case-studies/<key>.jpg`); blog and legal pages fall back to the site-wide `og-image.jpg`. `pageHead()` takes optional `ogImage`/`ogImageAlt`, and the alt text follows the image so a card naming one case study is never announced on another page.

**Favicon** is the real DigiBlu mark (`assets/favicon.png`), emitted as both `icon` and `apple-touch-icon`.

An SEO audit was run and its findings prioritised into a to-do list before any of it was actioned (the user explicitly asked for research first, action second). Two items have been done; the rest are tracked in Known follow-ups below.

- **Biggest lever, deliberately not touched: the single-URL architecture.** Every blog post, case study, and legal page only exists as a JS-populated modal, not a separate crawlable URL, and the content only renders into the DOM on click rather than being present on load — both cap how well any of it can be individually indexed or ranked. Fixing this properly means giving each piece its own pre-rendered page, which cuts against this project's "no build tooling, no framework" identity (see the top of this file) — a real architectural decision, not a quick edit, so it's flagged rather than done unilaterally.
- **`Organization` JSON-LD**  (`<head>`). The `FAQPage` block was removed with the FAQ section - leaving schema describing content no longer on the page would have been invalid structured data. The `@graph` wrapper was flattened to the single remaining node. `Organization` carries name, legal name, URL, `logo`, `image`, description, the real registered address (`PostalAddress`, matching the footer's `CM1 1NH` — the legal pages' `CM1 2NH` typo is a pre-existing discrepancy in DigiBlu's own published text, not something to "fix" here), and `sameAs` → the LinkedIn page.png` and `assets/og-image.jpg` are both `<head>`-only, so they have zero effect on the published artifact.** The rebuild script strips `<head>` entirely (Artifacts supply their own), so nothing in this SEO pass is visible on the claude.ai preview — it only matters once/if this ships to the real `digiblu.com`. Don't be surprised the artifact looks unchanged after this work; that's expected, not a bug.
  - Both images were generated via canvas in the browser preview, then decoded straight from the raw tool-result payload with Node (`Buffer.from(base64, 'base64')`) rather than hand-relayed through a written response — for `og-image.jpg` specifically, the base64 payload was long enough to overflow into a saved tool-result file, which turned out to be the safer path anyway (byte-exact, no transcription risk). Verify any future asset generated this way by checking the file's real signature and pixel dimensions (PNG: bytes 16–23 big-endian; JPEG: walk `0xFFC0`–`0xFFC3` SOF markers) before trusting it — don't assume a `toDataURL()` round-trip succeeded just because the tool call didn't error.
- **`og:image`/`twitter:image` now point at a purpose-built 1200×630 card** instead of reusing `hero.jpg` at whatever crop the browser happened to produce. Built from the real hero art (cover-cropped to the OG aspect ratio, not a new background), the same left-to-right scrim gradient the live `.hero` section uses, and the real DM Sans font loaded via `FontFace` before drawing — not a generic canvas fallback font. Added `og:image:width`/`height`/`type` and the previously-missing `twitter:title`/`description`/`image` (only `twitter:card` existed before).

## Content policy established this session

- **No fabricated named individuals.** Team = role titles, not people. This was a deliberate call after the site's public-sharing was initially blocked — the working theory was that a real company name + real address + real certifications + a non-functional lead-capture form reads exactly like a phishing page to automated review. A "Design prototype" banner plus de-attributed content were the fix at the time; sharing has worked since. (Testimonials were later replaced with real published client quotes, and **the banner and every other preview disclaimer were removed on request** when the site was finalised — see "Preview disclaimers removed" below. The no-fabricated-people rule still stands.)
  - **Real, published names are fine** — the case-study quotes name real people (e.g. Jon Willicombe at OVO, Jeremy Maynor at AssuranceSD) because those are published on digiblu.com, not invented. The rule is against *fabrication*, not attribution. Replacing invented content with verifiable client work helps the phishing-review problem rather than worsening it.
- **Real links only.** Header nav and footer links were fetched directly from the live digiblu.com (nav, services, legal pages, LinkedIn) rather than invented. In-page anchors (`#about`, `#services`, etc.) resolve to real section IDs; `scroll-behavior: smooth` with `scroll-padding-top` handles the jump.
- **The footer no longer navigates off the demo.** It originally sent visitors to the real digiblu.com in the same tab for every link, which was inconsistent with the rest of the site (blog posts, the LinkedIn icon — everything else external opens in a new tab) and meant one footer click could lose someone's place in the prototype. Now: **Company** links are in-page anchors in page order - Home, About Us, Case Studies - followed by Contact Us (Blogs was listed here until the blog was removed) (set on request; the **Services** column likewise follows the order of the six service cards, so keep both columns in sync with the page if sections or cards are reordered); **Contact Us** opens the same contact modal as the header CTA (`.js-contact-open`); **Services** links open the existing `#serviceModal` (the opener selector is `.service-learn-more, .footer-col button[data-service]` — footer buttons carry no extra class, so they inherit plain `.footer-col button` styling rather than the card "Learn more" look); **Legal** links (Terms, Privacy, Modern Slavery, Carbon Reduction Plan, Armed Forces Covenant) open `#legalModal` with the **full text** of each policy (`LEGAL_DETAILS` object) — not condensed, on explicit request. These are now real `<a href="legal/<slug>.html" data-legal="...">` (selector `.footer-col [data-legal]`, not `button[data-legal]` any more) rather than plain buttons — see Static pages above for why and the progressive-enhancement pattern (plain click still opens the modal). This is the one content type on the site handled differently from Blog/Case Studies: those are paraphrased because reproducing a third party's full article would be a copyright concern; a company's own legal policy text, shown on that same company's own prototype, doesn't carry that concern the way a news article does, and paraphrasing a legal document changes what it actually says. Pulled verbatim via `get_page_text` (raw DOM text, not an AI-summarized `WebFetch`, which would have paraphrased despite instructions) directly from each live page, structured by the source's own numbered sections (Terms: 18 items including intro, Privacy: 24, Modern Slavery: 8, Carbon Reduction Plan: 7, Armed Forces Covenant: 4). Numbered sub-clauses within one section are joined with a literal `\n` in the JS string (not real newlines — those aren't legal inside a single-quoted JS string) and rendered via `#legalModalList .service-modal-item p { white-space: pre-line }`, scoped to this modal only. The popup footer had a "Read the full policy on digiblu.com" link (`#legalModalReadLink`), then a `.modal-disclaimer` provenance line; **both are gone** — see "Preview disclaimers removed" below. `LEGAL_DETAILS[key].url` is still populated in the data (a provenance record for whoever maintains the policies) — don't be surprised it's unused if you go looking for where it's read. Only the LinkedIn icon still opens externally (`target="_blank" rel="noopener"`), because it always did and that's correct.
  - **All five policies are complete**, including two gaps found and closed on a follow-up pass: the Carbon Reduction Plan's "Actual tCO2e Emissions" section references two data tables that the live page renders as **images of a spreadsheet** (not real `<table>` markup or text), so `get_page_text` silently skipped them — fixed by downloading the images and transcribing the figures directly into `carbon.points[3]` (year-on-year tCO2e/employee/% savings 2022–2025, then a 2024 Scope 1/2/3 breakdown). The Privacy Policy's "Complaints" section references a "Table 1 overleaf" for complaints-handling contacts that **genuinely does not exist anywhere on the live page** (checked for table elements, images, iframes — zero) — this is a real gap in DigiBlu's own published policy, not an extraction miss, so `privacy.points[10]` restores the referencing sentence verbatim with a neutral parenthetical noting the table isn't present at time of writing, rather than fabricating contact details or silently dropping the reference.
  - **This prototype is expected to become DigiBlu's real production site**, which is the reason legal completeness was treated as a hard requirement rather than a nice-to-have — keep that bar (verbatim, complete, sourced from the live page) for anything added to `LEGAL_DETAILS` going forward, and prefer flagging a genuine source-side gap (like the missing Table 1 above) over inventing content to fill it.
  - `.footer-col a` and `.footer-col button` share one CSS rule so a visitor can't tell which mechanism a given link uses — same block layout, colour, no underline; the button reset is inert on real `<a>` tags. Verified: identical contrast ratio (6.1–6.3:1) between the two element types in both themes.
  - Desktop nav was also missing **Our Experts** (`#team`) even though the mobile menu had it — fixed earlier this session so both listed the same 7 items. **Later reduced to 5** (Home, About Us, Services, Case Studies, Our Experts) on request — Blog and FAQ were dropped from both the desktop `.nav-center` and the `.mobile-menu` (kept in sync with each other deliberately, same reasoning as the original Our Experts fix: the two nav lists should never diverge) and are now reachable only from the footer's **Company** column, which already had Blog and gained a FAQ link (`href="#faq"`) alongside it. Verified no wrap/overlap even at 901px, the narrowest width before the mobile breakpoint takes over.
    - **The nav now ends in Blogs, not FAQ.** FAQ was later re-added to the nav, then removed again along with the section itself; `Blogs` (`#blog`) took its place across the desktop nav, the mobile menu and all 20 generated pages. The footer Company column lists Blogs and no longer carries a FAQ entry.
- **Real content sourced from digiblu.com**: company description, all 6 service names/descriptions (plus each service's dedicated-page breakdown, used in the Learn More popups), all 8 case studies (client names, figures and published quotes), the blog posts (titles, dates, reading times and opening lines), NPS stat (+80 when sourced; DigiBlu has since updated it to +85, which is what the Values list shows), UK registered address, 6 certifications (added ISO 9001 and ISO 42001 — previously only 4 were listed), 5 legal page titles.
- **Article/case bodies are condensed, not copy-pasted.** Long-form source prose is paraphrased; only the short attributed client quotes and blog opening lines are verbatim. Keep it that way if you add more.
- **No em dashes in visible content**, on request — site-wide style choice, not a copyright/accuracy one. All 95 content-context em dashes (homepage copy, case studies, blog condensed points + full articles, legal policy text, including the transcribed Carbon Reduction Plan tables) were swapped for a plain hyphen (` - `, or `-` where the source already had surrounding spaces); left the 2 unspaced instances in the `no-code-low-code` article's full text (`approach—no-code`) with spaces added around the hyphen rather than a bare `approach-no-code`, which would misread as a compound word. Two occurrences used the `&mdash;` HTML entity rather than the literal `—` character (a static case-study card blurb) and needed a separate sweep — **check both forms** if you're auditing for stray em dashes later. The 25 remaining `—` in the file are all inside HTML/JS comments (developer-facing, not rendered), deliberately left alone. New content added later should stick to plain hyphens rather than reintroducing em dashes.

## Preview disclaimers removed (site finalised)

On request, **every "this is a preview / reproduced from digiblu.com" disclosure was stripped** so the site reads as a finished production website rather than a demo. Removed:

- The **`.proto-banner`** at the very top of every page ("Design prototype. This preview is not connected to a backend…") — markup gone from `index.html` and `generate-static-pages.js`, CSS rules gone from `assets/site.css`.
- All six **`.modal-disclaimer`** paragraphs: the contact form's "This is a design preview. No message is actually sent.", three "Content adapted from digiblu.com for this preview." lines (case index / case detail / service modals), the blog modal's condensed-vs-full note, and the legal modal's "Reproduced in full from the live policy on digiblu.com, which remains the current, authoritative version." The `.modal-disclaimer` CSS rule is gone too.
- The **JS that wrote the blog modal's disclaimer text** (`disclaimerEl` + the two `textContent` assignments in `showSummary()`/`showFull()`). Removing the element without these would have thrown a `TypeError` on every blog card click — worth remembering that these disclaimer paragraphs were not all static markup.
- The five **`LEGAL_DETAILS` intros** rewritten to drop the self-referential provenance ("The full Privacy Policy published on digiblu.com, reproduced by section." → "Last updated February 2024."), keeping the genuinely useful date/scope. On DigiBlu's own site, citing digiblu.com as an external source read as odd anyway.

**Dangling cross-reference fixed at the same time:** the Privacy Policy's "10. Complaints" section carried a parenthetical ending "…the link below to the live policy will carry it if that changes" — but that link (`#legalModalReadLink`) had already been removed in an earlier pass, so a legal document was pointing at something that did not exist. Now reads "(Table 1 is referenced above but is not currently populated.)" **When editing legal text, grep for references to other on-page elements** — this one survived two prior passes because nothing links the prose to the element it describes.

**Known, accepted consequence — the contact form.** `<form class="modal-form">` still has **no backend**: `submit` calls `preventDefault()`, adds `.sent` and shows "Request sent. Thanks for reaching out. A member of the DigiBlu team will be in touch shortly to confirm a time." Nothing is transmitted, stored or emailed. With the disclaimers gone there is now **nothing telling a visitor this**, so a real prospective client can complete the form (name, work email, phone, company, message, preferred time), see a success screen, and reasonably believe DigiBlu has their enquiry. This was flagged explicitly and the trade-off accepted in order to finalise the site; the agreed condition was that it is not shared publicly until a backend is wired. **Wiring that endpoint is the single highest-priority item before any public launch** — see Known follow-ups.

## Audit baseline (re-run these before shipping changes)

A full review was carried out and its findings fixed. The measured state afterwards, worth re-checking after any styling change:

- **Contrast: 0 failures in both themes** (was 6 dark / 64 light). Audit walks visible text, computes effective background, and must skip three things or it produces false positives: elements under a `background-image` ancestor (can't be computed numerically), elements under an `opacity: 0` ancestor (the inactive `.team-slice` labels are hardcoded white for the active blue slice and are legitimately invisible otherwise), and anything under `aria-hidden`.
- **No horizontal overflow** at 320 / 375 / 390 / 740 / 1440 / 1895px.
- **All tap targets ≥24px.** `.service-learn-more`, `.case-read-more` and `.case-back-btn` are inline-styled text links only ~17px tall — they use a transparent `::after` overlay to reach 44px **without changing layout**. Don't "simplify" that away by adding padding; padding shifts the surrounding rhythm.
- **All 5 dialogs** open/close/Escape cleanly, restore body scroll, and leave no stray `inert`.

## Deliberately NOT done (offered, declined or deferred)

- ~~**Sticky nav.**~~ **Done** — the nav is now fixed and frosted at every width (see Hero/nav above), so this is no longer outstanding.
- **Class naming debt.** The blog reuses `.cases-actions` / `.case-viewall-btn`, and the blog/case modals reuse `.service-modal-*`. Functionally correct, confusingly named. A rename touches CSS + HTML + JS across many places for zero user-visible benefit, so it was left alone rather than bundled into a large verified fix set.
- **Modal plumbing consolidation.** The contact and service modals still carry their own copies of the inert/focus-trap/Escape logic instead of `createModalController`. Same reasoning — pure-internal refactor, nonzero regression risk on the most important flow (the contact form). Worth doing as a focused standalone change.

## Known follow-ups (not urgent, just flagged)

- **Team photo sources are mixed resolution.** The active slice is 783px wide but the headshots are 560x560, so they upscale ~1.4x. Tracing the originals back to DigiBlu's Wix CDN (request `https://static.wixstatic.com/media/<id>` with no `/v1/fill/` segment to get the upload) shows they range 425x525 to 2000x2000 - so several could gain real detail, but **Vic's original is 558x464, smaller than what ships**, and two others are also at or below it. Re-sourcing would also allow a clean re-key rather than repairing the existing alpha.

- **SEO — remaining items** (see the SEO and Static pages sections above): every blog post/case study/legal doc now has its own pre-rendered URL with `Article`/`WebPage` JSON-LD (done — see Static pages), so what's left is mostly deployment-time: `robots.txt`/`sitemap.xml` exist (generated alongside the static pages) but are meaningless until this is on a real domain, not previewed as an Artifact; deploy the lightweight `index.html` + real cacheable asset files rather than the base64-inlined artifact build, and self-host Google Fonts there too (already true for the artifact build only, and for `assets/site.css` — the source `index.html`/generated pages still pull DM Sans from Google Fonts); author/`dateModified` schema on blog posts is low priority since digiblu.com doesn't publish author names for these.
- **Accreditation marquee placement** was raised and deliberately left alone: the recommendation was to keep trust badges directly under the hero (they answer "are these people legitimate?" while a visitor is still deciding whether to scroll) and, if the row feels heavy since it became image-based, shrink it rather than relocate it above the footer. Not agreed either way — revisit if asked.
- **BLOCKER FOR PUBLIC LAUNCH — contact form has no backend.** The `<form class="modal-form">` submit handler only shows a success screen; nothing is sent, stored or emailed. All disclaimers that used to warn about this have been removed (see "Preview disclaimers removed"), so the form now looks fully functional to a visitor and silently discards real enquiries. Wire the submit handler to a real endpoint (or swap the form for `mailto:`/`tel:` contact routes) **before this is shared with anyone outside the team**. The form collects first/last name, work email, optional phone (`#cf-phone`, `type="tel"`, no `required` — not format-validated since international numbers vary), company, message and preferred contact time; nothing currently reads any of those values.
- Team section is `aria-hidden`-free but uses **placeholder role-based avatars** (SVG icons, not photos) — swap in real people whenever DigiBlu wants named leadership on the page.

## Environment note

Browser-preview screenshot tooling has been unreliable across sessions (`the page is not compositing frames` / stuck mid-CSS-transition reads / hangs on `img.decode()` for lazy images while the pane is hidden).

**Read this before trusting a `getComputedStyle` result on any animated property.** Because the pane often isn't compositing, `requestAnimationFrame` never fires, so **CSS transitions freeze at t=0 and never advance**. `getComputedStyle` then returns the *start* of the transition, not the target — which reads exactly like a broken style rule. This cost real debugging time twice (the G-Cloud `filter` invert and the blog filter pill background both looked broken and were not). When checking a property that has a `transition` on it, set `el.style.transition = 'none'` and force a reflow first, then read. Waiting with `setTimeout` does **not** help — a frozen transition never completes. `file://` navigation also renders as a non-interactive static snapshot in this tool — use the `.claude/launch.json` dev-server config (`npx http-server` on port 4173) instead, so the Browser preview tool loads over real `http://`. Verification has leaned on `getComputedStyle`, `getBoundingClientRect`, canvas pixel-sampling for contrast, direct `fetch()`/network checks for asset loading, `querySelector`/`click()` simulation for interactive flows, and direct cascade/rule inspection instead of visual screenshots. If screenshot tooling is healthy in a future session, a straightforward visual pass is still worth doing — everything here was checked numerically, not eyeballed.
