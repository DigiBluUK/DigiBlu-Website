# Section-by-section notes

Every section of the home page, top to bottom, and the dialogs: how each behaves, what was tried and reverted, and the traps found on the way. Read the entry for a section before changing it.

> **Moved verbatim out of CLAUDE.md on 18 Sep 2026**, when CLAUDE.md was cut to a
> current summary on DigiBlu's developer's advice. These notes were written as the work
> happened: each is dated, and some describe states that have since changed.
> **Where anything here disagrees with CLAUDE.md, CLAUDE.md is current.** Since they were
> written: the site deploys on **vinext** (not OpenNext) with **pnpm** (not npm) and has
> **no CI**; the contact form sends email through Azure Communication Services; and the
> old static site is retired, so where a note names `index.html`, `assets/site.css` or the
> claude.ai artifact build, read `app/globals.css` and the components.
> **There are no dialogs since 18 Sep 2026** (built on the `pages-only` branch, merged into `dev`
> on 21 Sep 2026): every service, case study, bio, accreditation, legal document and the contact
> form is a page of its own, and `lib/client/modal.js` (`createModalController`) and
> `components/dialogs/` no longer exist. Where a note describes a dialog, the case-study reader or
> a dialog opener, it is history.

## Page structure

**Order changed on 8 Sep 2026 on stakeholder feedback: Hero, Accreditations, Services, Case Studies, Clients, About, Team, Footer.** The numbered entries below keep their original numbers and are no longer in page order - About (3) now sits between Clients (6) and Team (8). Only the markup moved; every scroll-driven section measures its own box, so nothing needed rewiring (verified: Services entrance fires, About values run 01 to 05, narrative fills 51/51). The desktop nav, mobile menu (`index.html` and `generate-static-pages.js`) and the footer Company column were reordered to match (Home, Services, Case Studies, About Us, Our Experts / Home, Case Studies, About Us, Contact Us).

1. **Hero** — full-bleed `assets/hero.jpg`, nav (desktop links + hamburger below 900px), theme toggle, headline (**"Applied AI, real ROI" on one line since 9 Sep 2026** - no closing full stop: every heading on the site lost its terminal stop on 9 Sep 2026 on team feedback, this one, "Who we are" and the three featured case-study headlines plus the eight `CASE_STUDIES` titles they mirror - it went in on 8 Sep as "Applied AI." over "Real ROI." and the full stops were read as misplaced; "Applied AI… Real ROI" was tried for an hour and the comma sentence chosen from the alternatives offered. Each half is a `<span>` held together with `white-space: nowrap`, so on a phone the line breaks only at the comma. Before that it was "Consulting judgment. Real technical depth."), CTA opens the contact modal. **The `.hero-bottom` paragraph was also rewritten on 8 Sep 2026** to "We pair operating model expertise with practitioners who’ve built the AI solutions themselves, so transformation delivers value fast and drives continuous improvement." - the supplied wording ended "keeps delivering"; "drives continuous improvement" is the wording DigiBlu settled on for the continuous-improvement ending. The small "DigiBlu" eyebrow above the headline was removed on request, and the nav wordmark is now the real logo image (`.logo-mark`, see the Files table).
   - **The artwork covers the whole section with the nav over it, and the strip behind the nav is calmed** (8 Sep 2026, second attempt the same day): a vertical fade on `.hero::before` (0.72 black at the top, 0.5 at `--hero-art-top`, clear 180px below) subdues the bright ridge under the nav links, and `.hero-spark` carries a matching `mask-image` so a light dims over 120px as it climbs into that strip instead of arriving bright under "Our Experts". **The first attempt started the art below the nav on a solid black band**; that read as the nav having a black background and was reversed on request. The paragraph below describes that first attempt and is kept for the mechanics that survive: the art lives on `::before` (now `top: 0`), `--hero-art-top` is `--nav-h` + 12px, `.hero-spark` must have the same box as `::before` with an explicit height, and the section base colour is fixed black. **`.hero` must never become a stacking context** (no `isolation`, no `z-index`): `.site-nav` is a fixed-position child of `.hero`, and a stacking context there traps it so every positioned block further down (the service cards) paints over the nav once the hero has scrolled away - reported as the nav disappearing on Services, introduced the same day by an `isolation: isolate` added for the art layer. The art `::before` is `z-index: 0` and `.hero-center`/`.hero-bottom` are `position: relative; z-index: 1` instead.
   - **Superseded the same day (kept for the mechanics) - the artwork started below the nav** (8 Sep 2026): the bright ridge in `hero.jpg` crested exactly where the nav links sit and was reported as the picture touching the menu. The art (scrim + photo) moved from `.hero`'s own background to `.hero::before`, offset by `--hero-art-top` (`--nav-h` + 12px, so it tracks the measured nav on every device), over a fixed `#000` band the nav sits on - fixed because the nav's over-photo controls hardcode white. `.hero-spark` takes the same inset so its `slice` maths still align with the art; the pseudo was `z-index: -1` under `isolation: isolate` on `.hero` - both since replaced, see the nav note above. The rebuild script's `url('hero.jpg')` replace is unaffected (one occurrence, now on the pseudo).
   - **The artwork is `assets/hero.svg`, a vector rebuild of `hero.jpg`, since 8 Sep 2026** (feedback: the background looked blurred and pixelated - it was, see the Files table for the measurements and the tracing method). Verified against the JPEG by rasterising both at 1672x941: mean per-channel difference 2.3/255, 0.3% of pixels differ by more than 40, and the luminance profile across a rim matches within a few units (rim line 2.6px, a 3px bright shoulder inside it, then the slow fall to the band colour). The three `.hero-spark` paths are now derived from the same geometry - see the spark note below. The `<link rel="preload">` in `<head>` points at the SVG. Same coordinate space, so nothing else moved. **If the artwork is ever edited, edit the SVG** - re-tracing from the JPEG is a fallback, not the source of truth now.
   - **`.hero-logo` sits above the headline at every width.** It was mobile-only at first (the nav carries the same lockup on desktop), then brought back everywhere on request. A separate class from `.logo-mark` on purpose: that one is cropped to icon-only below 900px for the nav bar. Sized on the mask's exact 4.1184 aspect - the mask has zero transparent padding (verified by decoding its alpha), so the box IS the visible mark and any drift letterboxes it. Desktop 148.3x36, mobile 115.3x28. The bottom margin is a *box* gap, not the optical one: the h1's cap-top sits ~6.8px below its line box, so 15px renders as ~22px of real whitespace. `background-color` must stay `var(--text)`, **not** `currentColor` - `.hero` pins white ink for its children, but `color` is computed on body from the root's `--text` and merely inherits, so `currentColor` resolved to near-black and the mark vanished on the hero art in light theme.
   - **The nav's Get in touch (`.btn-signup`) has no border since 10 Sep 2026.** It had `border: 1px solid transparent` over the brand fill; with `background-clip` at its default the fill should paint under the border, yet over the hero art the edge read as a lighter ring and was asked off. `border: 0` with the padding grown by the same 1px, so the button keeps its size. The hero's own `.btn-started` is the white button and was never outlined.
   - **The nav is fixed and frosted.** `.site-nav` is a full-width `position: fixed` positioner; `nav` inside it is the max-width content row. At rest it is fully transparent over the hero photo; past 24px of scroll a `.scrolled` class turns `nav` into an **inset, rounded glass panel** — `rgba(var(--glass-rgb), 0.68)` + `backdrop-filter: blur(12px)` (was `blur(18px) saturate(160%)` until the 2 Sep 2026 scroll-smoothness pass: backdrop-filter cost scales with area x radius^2 and runs during *all* scrolling, so it was the heaviest single effect for a weak GPU at high DPI - a Surface Pro X stuttered; 12px without the saturate looks near-identical at roughly half the cost), inset `--gutter` from each edge with a 12px top margin — so page content is visibly moving beneath it. The glass lives on `nav`, not `.site-nav`, which is what keeps it wrapping the nav options rather than painting edge to edge (it was a solid full-width bar first; that read as "a wide white background" and was changed on request).
   - **0.68 opacity is a floor, not a taste call.** The panel floats over arbitrary content, so its text contrast cannot be computed statically the way a flat surface's can; that opacity keeps labels legible even over a dark image scrolling underneath. There is an `@supports not` fallback to a near-opaque surface where `backdrop-filter` is unavailable.
   - **`.scrolled` must re-point `--ink-rgb`/`--text` at `--page-ink-rgb`/`--page-text`.** `.site-nav` is a DOM descendant of `.hero`, which pins white ink for everything inside it regardless of scroll position, so a plain `var(--text)` here would still resolve to hero-white and be invisible on light-theme glass. `.logo`/`.theme-toggle`/`.nav-toggle` additionally hardcode `#fff` (for the transparent over-photo state), so they need their own explicit override — redefining the variable does not reach them.
   - **`--nav-h` is measured from the live nav by script, not hardcoded**, and drives both `.hero`'s `padding-top` (the nav is out of flow now, so the headline would otherwise ride up) and `html { scroll-padding-top }` (`+26px` to clear the glass panel's top margin). That scroll-padding is what fixed the "lots of white space above the section when the page settles" report.
   - **Since 8 Sep 2026 the three light paths are the fitted rim geometry of `hero.svg`** (rims 2, 3 and 0: bottom edge, tangent arc, then the top edge to where each original trace ended), so a light is on its rim by construction - 40 of 40 samples on each - and the `path` attributes shrank from ~1400 characters of traced polyline to one line-arc-line each. Derived in the scratchpad from the same seven rim paths the SVG is built from; if `hero.svg` changes, re-derive these. The paragraph below describes how the paths were first traced from the JPEG and is kept for the method.
   - **`.hero-spark`** — small lights travel back and forth along the bright diagonal bands baked into `hero.jpg`, on request ("a star like light going around the lines of the blue arrows"). Paths are not eyeballed: `hero.jpg` was drawn to a canvas and each bright ridge traced by sampling actual pixel brightness (`(r+g)/2` — the highlight edges are near-white so R and G both spike, while the deep-blue fill stays low on both) with a perpendicular-recentring ridge-follower. The SVG overlay uses `viewBox="0 0 1672 941"` (hero.jpg's native size) with `preserveAspectRatio="xMidYMid slice"`, which reproduces the background's `center / cover` crop math exactly — every path stays pixel-aligned with the artwork at every viewport size, including the heavy crop on narrow/tall phone viewports (where much of a path simply scrolls out of the visible slice, same as the art itself). Motion is plain SVG SMIL (`<animateMotion>`, `keyPoints="0;1;0"` for a smooth there-and-back with no end-of-cycle jump) on pairs of `<circle>`s (a soft radial-gradient glow + a small bright core) — no JS animation loop, no per-frame `<feGaussianBlur>` filter (the glow comes from the gradient definition, not a runtime filter), so it's compositor-cheap. `.hero-spark` is `position:absolute; z-index:0; pointer-events:none`, placed as the first child of `.hero` so it paints behind the flex-item nav/headline/CTA (verified via `elementFromPoint` click-through checks, not assumed); hidden under `prefers-reduced-motion: reduce`.
     - **Three lights, not one**, on request ("not just one line of movement, it's happening on the other lines too but maybe one or two at a time") — three separate bands traced the same way, each with its own `<g>` wrapping its glow+core circle pair. Position (`<animateMotion>`, 12s ping-pong per light, `begin` staggered by a few seconds per light so they don't move in lockstep) is independent from **visibility**, which is gated by a separate `<animate attributeName="opacity">` per `<g>` on a longer, shared 24s envelope (`values="0;1;1;0;0"` — fade in, hold, fade out, stay off) with each light's envelope offset by 8s (`begin="0s"/"8s"/"16s"`). The three 24s windows overlap by design, so at any moment roughly 1-2 lights are visible, never all 3 — verified by seeking `setCurrentTime()` across a full 24s cycle at 1s steps and counting how many `<g>`s had `opacity > 0.5` at each sample: 0 lights only at the single t=0 boundary instant, 1 light for 14 of 25 samples, 2 lights for 10, **never 3**.
     - **Verifying this needed a different technique than the rest of the site.** The preview pane reports `document.visibilityState === 'hidden'` here, which throttles real-time SMIL/CSS timing to a full stop (`svg.getCurrentTime()` stays at `0` indefinitely) even though the element isn't actually broken — a stricter version of the frozen-transition issue in the Environment Note below. Confirmed the wiring is correct instead by seeking the clock directly with `svg.setCurrentTime(t)` at several `t` values and reading `getBoundingClientRect()`/`getComputedStyle().opacity` after each seek: the original single light's positions traced a clean symmetric round trip (t=3 and t=9 landed on the same point, as did t=1.5/t=10.5 and t=0/t=12), and the three-light opacity envelopes showed exactly the staggered 1-2-visible pattern described above — neither can be demonstrated by real-time playback in this environment, but a direct seek can.
2. **Accreditations** (`.accred`) — a **written, auto-scrolling single line** of the 6 real certifications, anchored by a standing "Accredited & certified" label so it reads as a claim rather than a decorative ticker. Each chip is `NAME • Plain-language scheme` (ISO 9001 • Quality Management, … UK Government G-Cloud • Approved Supplier). This replaced a badge-image marquee on request ("change from badges to written, easier to read"), and the badge *masks* under `assets/badges/mask/` are consequently no longer referenced by anything — only the full-colour originals survive, for the popup.
   - **It scrolls, matching the Clients carousel**, on follow-up request ("make it move similar to the way the client logos are moving, so it's all in one line"). Same mechanism: two identical `.accred-group`s in a `.accred-track` animated to `translateX(-50%)`, paused on hover, disabled under `prefers-reduced-motion`. Both groups **must stay exactly equal width** or the loop visibly jumps at the seam (verified 1627px each).
   - **One repetition per group is enough here**, unlike the old badge marquee which needed two. The six text chips total ~1620px against ~1310px of available track at `--maxw`, so a single set already overflows the viewport and leaves no gap. Re-measure if the chip text is ever shortened.
   - **Every visible copy is clickable**, opening `#badgeModal` with the real full-colour artwork plus a description of the scheme (`BADGE_DETAILS`). Only the **first group is in the accessibility tree and the tab order**; the duplicate group is `aria-hidden` with `tabindex="-1"` on each button — still clickable (the strip moves, so whichever copy is under the cursor is arbitrary), without doubling the tab stops or announcing every certification twice.
     - **Clicks are delegated on `.accred-track`**, not bound per-chip, for exactly that reason. A per-button version would leave the six duplicate chips looking identical but doing nothing — the same bug the old badge strip had.
   - **The description is centred under the centred badge and title** (8 Sep 2026; it was left-aligned and read as pushed to one side under ISO 14001). The team profile dialog shares the layout and its bio was centred with it.
   - **The modal shows the original full-colour badge.** `#badgeModalVisual` is a plain `<img>` sized to its natural dimensions (`max-width:100%; max-height:128px`). Each `BADGE_DETAILS` entry's `img:` field points at the **original** `assets/badges/*.png`. G-Cloud's original is white line-art on transparency with no colour of its own — its entry alone sets `onDark: true`, toggling `.on-dark-plate` (`#0a0e1a`, theme-independent) so it stays legible in both themes, matching how DigiBlu's own site only ever shows that mark on a black band. These originals are a real dependency of `$body` in the rebuild script.
3. **About** (`#about`) — "Who we are" heading (no full stop since 9 Sep 2026), then the narrative at full size in one column (**860px measure since 8 Sep 2026, down from 940, with the dot figure at 300px starting 64px beside it and the two centred on each other** - the figure had been centred in a 430px end track, 122px from the text and 40px short of the right edge, and hung below the copy; asked to be brought in and aligned) (a short blue `.about-rule` sat under the heading until 3 Sep 2026 - removed for consistency, since no other section carries a decorative rule), then the values beneath under an **"Our values"** label. The label-left / statement-right arrangement built from a supplied reference was **reverted on request**; the copy it introduced ("At DigiBlu, we bring together...") was kept, since the scroll reveal was specified against that wording. **This section has now been restacked, re-columned and restacked again - do not change its arrangement without being asked.**
   - **8 Sep 2026: on desktop all five values are open at once, three across then two, and the sticky hold is gone.** Repeated feedback was that the scroll-driven one-at-a-time accordion went past too fast to read. The `min-width: 761px` block is now a 3-column grid with every `.value-text` shown, numerals at full ink and a 64px cap (down from 82px, since five share the row), the buttons inert (`cursor: default`) but still real buttons for the phone column, **with the brand-blue hover/focus tint on numeral and hairline kept** (dropped with the accordion, asked for back the same day). The values IIFE checks `(min-width: 761px)` per scroll event: on desktop it opens everything once and returns, and clicks are no-ops; below it the cumulative scroll reveal and tap behaviour are unchanged. **The desktop pin (`.about-inner` sticky + 85vh `.about-pin-space`) was removed with it** - it existed only to pace the accordion; the spacer stays in the markup at `display: none` so `__aboutPinProgress.live()` reads false and the narrative fill uses its viewport mapping. Everything below about the accordion, the pin and the reveal order describes the phone column or is history.
   - **The narrative fills fast: its top travelling from 92% to 50% of the viewport, about 0.4 of a screen.** It ran until the whole block had cleared the upper third (roughly a full screen), and a stakeholder scrolling at speed was past the section with the copy still grey (reported 2 Sep 2026). The word transition dropped to 0.18s with it, since a longer one visibly trailed the words it was lighting. The pin term now completes by 6% of the pin, a safety net rather than the mechanism.
   - **The narrative fills to full ink as it scrolls.** Each word is wrapped in a `.about-word` span by the about IIFE (walking text nodes, not rewriting `innerHTML`, so inline markup survives) and gains `.is-read` as the block travels up the viewport. **The filled state is `--text`, not a literal white**: white is only correct in dark theme and would be invisible on the light background. The unread state is `--ink-a-mid`, a token so it rises in light theme - it measures **5.3:1 dark / 5.7:1 light unread, 21:1 / 18.9:1 filled**, so the copy is legible *before* it fills, not only after. An earlier version of this effect filled to brand blue and was removed; this one fills to full ink and was asked for.
   - **The muting only applies once the script adds `.is-reading`**, so with JS off, or under `prefers-reduced-motion`, the paragraphs sit at full ink and nothing is lost but the effect. The scroll handler is deliberately **not** rAF-throttled - one rect read plus a walk over only the words whose state changed, and rAF is throttled to a stop in the preview environment.
   - **Values are an accordion of large numerals** (`.value-list` / `.value-item` / `.value-open`), built to a second supplied reference and **kept through the revert**: the open value shows its title and description beside a full-ink numeral, the rest collapse to a muted numeral under a hairline rule. One open at a time on desktop, 01 open on load, and **clicking the open one is a no-op rather than a collapse** - with nothing open the row is five bare numerals. Which one is open is now driven by scroll (below). The `.about-values-title` label above it is what says these are values; the numerals alone do not.
   - **Click, not hover.** The reference expands on hover; hover has no touch equivalent and cannot be reached from the keyboard, so hover and focus only tint (brand blue). Real `<button>`s carrying `aria-expanded`.
   - **Numerals have a fixed width (1.32em), because DM Sans ignores `tabular-nums`** (8 Sep 2026): it has no tnum feature, so "01" rendered 19px narrower than "04" at 64px and the titles beside them did not line up down a column (reported). The em width scales with the clamp and clears the widest pair; re-measure if the numerals ever go past 05.
   - **Title and numeral are top-aligned at every width, and the pad is measured.** Mobile used `align-items: baseline`, which put the 15px title's baseline on the 34-46px numeral's and dropped the title's top 14px below the numeral's - reported as the text sitting lower than the number. Both widths are `flex-start` now; the top pad on `.value-text` is what lines the cap heights up, and it comes from canvas glyph metrics (DM Sans cap top sits 10px below the box top at 82px but only 3.5-6px at 34-46px, against the title's 3.7px): 6px on desktop, 1px on mobile. Re-measure rather than eyeball if either font size changes.
   - **Hover has to outrank "revealed but closed".** Reveal is cumulative, so on 05 every earlier numeral carries `.is-revealed`, and the desktop rule keeping those faint (four classes) beat the plain hover rule (three): hovering 01-04 from 05 did nothing, while from 01, where nothing below is revealed yet, it turned blue - which made it look like a per-item bug (reported 2 Sep 2026). The hover/focus rules at the end of the desktop block carry five classes for exactly this reason; do not "simplify" them.
   - **The values row's bottom padding is 4px, not 34.** The hairline sits on top of each value, so bottom padding is pure air at the foot of the section, inside the standard 2 x --section-y gap - it made the values-to-Services gap read as 178px against 144 everywhere else. On mobile only the last item is trimmed; the rest of the padding is the spacing between values.
   - **Collapsed numerals sit at `--ink-a-faint`**, not the flatter grey the reference uses. They are the only visible label on a real button, and at that weight they measured 1.8:1 - decoration, not a control. The token gives 3.7:1 dark and 5.0:1 light, clear of the 3:1 bar for text this size, while still reading as plainly receded beside the open one at 21:1.
   - **On desktop the section pins while the values run.** `.about-inner` (a wrapper added for this) goes `position: sticky` and `.about-pin-space`, an empty spacer after it, supplies the 85vh of scroll the hold consumes (130vh until 2 Sep 2026) - a sticky element is constrained by its parent's **content box**, so padding on `.about` could not have supplied it. The *whole composition* sticks, not the list alone: heading, narrative, figure and values together, so the hold reads as the page pausing on About rather than a bare list floating in an empty screen. Nothing is hijacked - no `preventDefault`, no scroll listener moving the page - so a flick, a scrollbar drag and PageDown all behave normally and the user is never actually stuck.
     - **Centred in the viewport during the hold, not pinned under the nav.** The composition is ~734px and a viewport may be 1000px or more; held at the top, every spare pixel was one empty band beneath the values, which a stakeholder read as "quite a bit of space" between About and Services. The true section gap at release is the standard 2 x --section-y; the space was the pin's runway. Centred (`top: max(nav + 18px, (100vh - --about-inner-h) / 2)`, height written by the pin IIFE before it reads the top), the spare splits above and below and reads as a held slide. The hold was also cut from 130vh to 85vh for the same reason.
     - **Gated on viewport HEIGHT as well as width** (`min-width: 901px and min-height: 860px`). A sticky element taller than the viewport pins its top and puts its own bottom permanently out of reach; the composition measures ~734px, so a short screen drops the pin entirely and falls back to the unpinned mapping below. Also dropped under `prefers-reduced-motion`, where the values are all revealed at once and the hold would be 130vh of nothing happening.
     - **The pin froze the narrative fill, and that had to be fixed with it.** The word-by-word fill is driven by the block travelling up the viewport - which is precisely what the pin stops, at 41 of 51 words, leaving ten words visibly muted for the entire hold. The remaining fill now rides the pin's progress and completes by 18% of it, so the sentence finishes just as 02 opens. Anything else keyed to this block travelling will break the same way.
     - **Both handlers share one `window.__aboutPinProgress`**, defined in its own IIFE. It measures off `.about-pin-space`'s top edge, because sticky does not move an element in flow - the spacer keeps travelling at scroll speed while the composition looks frozen, which is the only ruler still moving. `.live()` reports whether the spacer has any height, so the script never restates the media queries the CSS owns. **The fill IIFE reads it off `window` per tick rather than capturing it**, because that IIFE runs *before* the one defining it - captured once, it would be `undefined` forever and the fill would silently keep freezing.
     - The sticky offset is cached and refreshed on resize, not read per scroll event: `getComputedStyle` in a scroll handler forces style resolution on every tick. **It is also re-measured once on the first scroll**, because `--nav-h` is set by the nav script further down the file - at IIFE time the sticky top still resolves to its 75px fallback, which was only correct while the nav happened to be 75px tall (found in the 2 Sep 2026 review).
     - **`.about > h2` became `.about h2`.** The new wrapper sits between them, so the child combinator stopped matching and the section title dropped to browser-default sizing - the same trap as the missing `.detail-page h1` rule. Check direct-child selectors whenever a wrapper is introduced.
   - **Scrolling drives the list, so it runs 01 to 05 on its own** and has finished before Services arrives. The band is measured from `.value-list`'s own box - it starts as the list reaches 82% of the viewport and ends with the list's *bottom* at 55% - which is what makes the short desktop row and the much taller phone column both complete inside the section. Ending on the bottom rather than the top is load-bearing: mapped off the top, 05 on a phone would only land once Services already filled the screen. Each value's turn then comes as it rises through the lower half (measured: every item opens while its own top sits between 82% and 60%).
     - **The `!!` in `setOpen` is load-bearing.** `classList.toggle(name, force)` with an *undefined* force flips the class instead of removing it. The keep expression is undefined for items beyond `n` whenever `keep` is not passed, so for a day every scroll event flipped 03/04/05 on and off - "revealing out of order" on a phone, 77 non-prefix states in a scroll-up sweep. Coerce to a boolean; verify with a fine scroll sweep that the revealed set is always a prefix.
     - **A tap only ever adds to the reveal.** `setOpen(n, keep)`: the scroll driver passes nothing and owns the reveal in both directions (down reveals cumulatively, back up takes it away - intended); a tap passes `keep`, so items already revealed stay revealed. Without it, on a phone with all five showing, tapping 03 quietly faded 04 and 05 out (reported 2 Sep 2026).
     - **Clicking still works and the next scroll takes it back.** The handler must also write the scroll memo (`last = i`), not just call `setOpen`. It did not at first, and the bug was exactly what a memo bug looks like: click 05 near the top of the band, scroll a little, and the comparison against a stale `last` short-circuits, so the clicked value stays open until a band boundary is crossed. Verified by clicking 05 at `f=0.05` and scrolling to `f=0.06`.
     - **Under `prefers-reduced-motion` the scroll driver is not installed at all** - every value is revealed and 01 opened. Nothing on this site may be reachable only through a gesture that never happens.
   - **Small screens reveal cumulatively; desktop stays one-at-a-time.** This is the one place the two widths genuinely behave differently, and it is forced by the layout rather than chosen: a horizontal accordion has room for exactly one open value, because a collapsed item has to surrender its width for the open one to grow into. A phone has no such constraint, so each value fades in as its turn comes and *stays*, and the section ends with all five readable without a single tap. `.is-revealed` carries this; at 761px and up the CSS reduces it back to a marker with `:not(.is-open)` overrides.
   - **On small screens the copy fades, it does not collapse** - `opacity`, with every value in flow from the start. Collapsing would grow the list as each value appeared, and the list's own box is what the scroll position is measured against, so the reveal would be moving its own trigger. Height measured constant at 650px across the whole sweep. **Above 761px it is `display: none`** for the width reason above - a collapsed item must not reserve the space its copy needs.
   - **Below 761px it is not an accordion**: a stacked list, now revealed by scroll rather than tapped open. It was a plain always-expanded list, then briefly a real accordion (which is what made a phone read as "01, 02, 03, 04, 05" with everything hidden behind a tap - reported), and is now the cumulative reveal. Scoped the way the team strip scopes its expanded card, so the phone layout cannot inherit a rule assuming horizontal room.
   - **Verifying any of this needs synchronous dispatch.** `window.scrollTo` in the preview pane coalesces its scroll events, so a loop of scroll-then-read returns the *final* state at every sample and the sequence looks stuck. Follow each `scrollTo` with `window.dispatchEvent(new Event('scroll'))`, and force `scroll-behavior: auto` first (a reload restores smooth, which never advances here). Reading the reveal also needs the `opacity`/`color` transitions disabled - the frozen-transition trap in the Environment note; without that, every newly revealed value reads as `opacity: 0` and the whole feature looks dead.
**The About figure** (`.geo-figure`) is a **dot lattice** in which the DigiBlu **db is rendered by dot size alone** - a halftone. Generated by `scratchpad/geo/dots.cjs`. 900 circles on one exact 30x30 pitch; 78 of them carry the mark, **and those 78 are emitted a second time** (since 9 Sep 2026) in a `<g class="mk-lit">` filled with the brand sweep - see `--brand-ink` in the Design system - so the cursor script sees 978 circles, the copies sharing the ink dots' classes so they pulse and part in step.

- **Why it is dots and no longer dashes.** The dash version carried the mark in each dash's ANGLE, turning to follow the local contour of a letterform. That is inherently ragged - the strokes are curved, the grid is not, and a curve approximated by grid-aligned ticks reads as noise well before it reads as a letter. It was reported as "messy" and no amount of blur or threshold tuning would have fixed it: the representation was wrong, not the parameters. On a lattice where every dot is the same shape on the same pitch and **only the radius varies**, there is no angle for anything to disagree about, so it cannot look untidy. Keep that property in mind before adding any per-dot rotation or jitter.
- **Radius comes from supersampled coverage, not a point sample.** Each cell asks what fraction of its own area the letterform covers, so an edge dot is genuinely part-sized rather than snapping between full and nothing. That is what keeps the outline smooth at this pitch; a single sample per cell produces a visibly stepped edge. **Only mark dots take that radius** (8 Sep 2026): coverage used to size every dot, so 54 dots with up to half coverage sat around the outline and inside the counters, enlarged but still in the field's faint ink - grey blobs beside the blue mark, reported as a glitch. A dot that does not make the mark now stays at the field radius; the mark's own dots keep their coverage-graded sizes, so the edge stays smooth.
- **The mark pulses less than the field** (`geo-pulse-mark` at 0.95-1.06 against `geo-pulse` at 0.82-1.14) and sits at full ink against the field's 0.34. At the field's amplitude the mark's edge visibly crawls.
- **The mark phases into the brand sweep with the scroll**, rather than switching at a threshold. `--mark-lit` is a 0..1 value the geo IIFE writes from the figure's position in the viewport - fully lit with it centred, easing back to ink as it leaves - and since 9 Sep 2026 it is the OPACITY of the gradient-filled copy of the mark layered over the ink one (it was a `color-mix` into flat `--blue`; a gradient cannot be mixed, so it is layered). It runs on every device: scrolling is the trigger everywhere, and hovering (where there is a pointer) forces it to full.
  - **Both go through the same inline variable**, not one as a CSS rule. An inline style outranks a stylesheet rule, so a `:hover` selector setting `--mark-lit` would never have won against the scroll handler writing it inline.
  - **`color-mix` sits behind `@supports`**, with a plain `fill: var(--text)` outside it, so a browser without it gets a legible mark in full ink rather than no fill at all.
  - **The value is quantised to 0.02 and written only on change, and `fill` transitions on hover only** (`.is-hovering`, kept for a beat after leaving so the way out fades too). Before, every pixel of scroll wrote a fresh value and restarted a fill interpolation on all 78 mark dots - paint work for a change no one could see. Fifty steps across the fade is far below what the eye resolves in a colour mix.
- **Tablet hardening (9 Sep 2026)**: a glitch was reported on a Surface Pro X that a desktop does not show, with no further detail. Two changes, both in the direction the earlier scroll-smoothness work already took. `.geo-figure` is `will-change: transform; contain: paint`, so the pulse's per-frame repaint stays inside its own compositor layer instead of dirtying the section around it. And a **frame-time probe** in the geo IIFE runs once, the first time the figure is on screen with the page still (the pulse only runs then, so that is the cost being measured), over 1.5s of frames: under ~38fps it adds `.is-lowend`, which stops the pulse and every circle transition and makes the cursor loop stand down, leaving the mark lighting on scroll. It needs a dozen real frames before it will say anything - a hidden tab or the preview pane throttles rAF to nothing, and silence is not slowness - and it restarts its window whenever `.is-scrolling` or `.is-offscreen` is set. Unverifiable here: the preview pane never delivers frames, so the class was checked for absence on the desktop and the logic by reading, not by a slow device.
- **The pulse is paused while scrolling and while the figure is off-screen** (`.is-scrolling`, cleared 160ms after the last scroll event; `.is-offscreen`, from an IntersectionObserver with an 80px margin). This is the scroll-smoothness fix that matters: Chromium does not composite transforms on individual SVG elements, so 900 circles animating `scale` is a full software repaint of the figure on every frame, sitting inside a sticky composition that is itself moving. A Surface Pro X stuttered through About; a desktop absorbed it. **The six scroll handlers were measured first and cost 0.22ms per event**, which is what ruled out scripting and pointed at paint. A 6s breath at 14% frozen during a scroll is invisible; the per-frame repaint was not.
- **The cursor parts the field as well as swelling it.** Field dots reach 3.4x and are pushed up to 9 viewBox units away from the pointer, across a 150-unit radius - measured 468 dots responding, 107 of them past 2x. An earlier version scaled to only 1.75x with no displacement and was reported as having lost the life the dashes had: on 1px dots, scale alone reads as almost nothing.
  - **Displacement is uniform; only scale differs.** Every dot, mark included, is pushed the same 9 units, so the lattice parts as one surface. The mark moved only 2 units for a while and the figure read as a layer of dots over a logo that ignored the cursor (reported 3 Sep 2026). Scale stays split because a mark dot is `r=3.15` against the field's `1.05`: at the field's 3.4x it would be 21 units across on a 12.28-unit pitch and the letterforms would blob together under the pointer. 1.9x lands at 11.97, just inside the pitch. **Re-derive it if `R_MARK` or the pitch in `dots.cjs` changes.**
  - **Verifying it needs an awaited rAF.** The response is scheduled through `requestAnimationFrame`, which the preview pane throttles to a stop, so a real hover followed by an immediate read reports zero dots moved and looks broken. Dispatch `pointermove` and await a frame before reading.
- **The cursor scales dots rather than turning them** - there is no angle on a circle. Written to the `transform` property while the idle pulse animates `scale`, so the two compose; sharing one property would silently do nothing, since a running animation beats an inline style. The falloff is smoothstepped, or the edge of the radius shows as a circular seam.
- Earlier versions kept in the scratchpad: `flow.cjs` (the dash field, in both its vortex and mark-only forms), `gen.cjs` (dissolve blocks) and `genlines.cjs` (cross-fading line figures). All replaced on request, not abandoned.


- **The field draws the DigiBlu "db" itself.** Not a logo laid over the pattern - an outlined mark was tried that way first and rejected. The dashes bend to follow the letterforms, so the mark emerges from the flow. **The mark is the db only, not the surrounding circle**: the icon is a filled disc with the db knocked OUT, so the letterforms are the holes, and a flood fill from the border separates "outside the disc" from "inside a letterform".
- **The blurred mark has to reach well past the letterform.** Its gradient runs across a stroke, so the stroke direction is perpendicular to it - but the strokes are thin against a 30x30 grid, and only ~57 dashes sit on one. A wide blur (37 passes) bends the surrounding flow too, which is what makes the shape read; a tight blur was tried and came out as noise. A second, narrower blur controls how strongly the mark wins over the vortex.
- **Directions blend as DOUBLED unit vectors.** A dash at a and at a+180 is the same dash, so averaging raw angles lets opposite directions cancel to nothing.
- **Dashes on a stroke run longer** (16 units against 10-11), so the letterform gains weight as well as direction.
- **The angle field is a sum of point vortices**, each contributing a velocity perpendicular to the line joining it and falling off with distance. **Crossed sinusoids were tried first and rejected** - they gave a uniform diagonal hatch with no swirl centres, which is the whole character of the reference. A softening term keeps the centres from blowing up.
- **Each dash is drawn already at its own angle**, so the animation only adds a sway and never composes with a base rotation, which SVG makes awkward. `transform-box: fill-box` pivots each dash about its own centre.
- **Twelve delay buckets, assigned by position at generation time**, so the sway crosses the field as a wave rather than every dash moving in lockstep. Buckets rather than per-dash inline delays: 361 inline styles is a lot of markup for the same result.
- **Theme-aware, not a pinned plate.** It was fixed black with white strokes at first, which left a black square on the white page in light theme. Strokes now follow `--text` and the plate `--bg`: white on black in dark, black on white in light, with the plate invisible against the page either way (measured 21:1 and 18.9:1).
- **The cursor curls the field.** Dashes within 118 viewBox units turn toward the tangent of a circle around the pointer, strongest at the cursor, easing back on leave. Written to the `transform` property while the idle sway animates `rotate`: the two compose, and **sharing one property would have silently done nothing** - a running CSS animation beats an inline style. The angle difference is folded into -90..90 because a line reads the same at a and a+180; without that the far side of the cursor spins the long way round. Centres and resting angles are measured once at startup, and only dashes inside the radius (plus any leaving it) are touched per frame. Skipped under `prefers-reduced-motion` and on touch, where there is no hover to track.
- **361 animated dashes.** It was 722 while two arrangements were live. Disabled under `prefers-reduced-motion`, which holds the field still.
- Earlier versions kept in the scratchpad: filled dissolve blocks (`gen.cjs`) and three cross-fading line figures on a 9s cycle (`genlines.cjs`). Both were replaced on request, not abandoned.

**The section's bottom padding is 40px on desktop, not `--section-y`, since 10 Sep 2026** (`.about { padding-bottom: 40px }` inside the `min-width: 761px` block). The gap to Our experts was reported as too big although it measured the standard 144px box to box: at wide widths the last row's copy is two lines and ends 31px above its own box (the 64px numeral sets the row height), the hairlines sit on top of each value so the block has no closing edge, and row two leaves its third cell empty, so the standard read as ~175px of air. At 40px the last line of copy sits 143px above the pill - the standard - and the numeral 118px. The phone column trims its own last item and keeps `--section-y`.

**The narrative is two paragraphs since 8 Sep 2026** (the practitioners statement, then "Our vision ..."), on request, at `clamp(20px, 2.05vw, 28px)` - a step down from the single statement-size paragraph, also on request - measure 940px. The fill walks every `.about-lede`, so the split needed no script change; verified 51 of 51 words still wrap and light. Before that it was a single paragraph at `clamp(23px, 2.4vw, 33px)`. It was three paragraphs, then three condensed ones, then merged into one, then trimmed again to drop the technology-agnostic clause: 51 words over 7 lines. That clause survives in the footer and meta descriptions, which are separate copy and still accurate.

**The A/B is over.** Three About Us arrangements were built and compared - the stacked one, a statement-with-flow-row one, and a split light/dark panel. **The stacked arrangement (`#about`) was chosen; the other two are deleted**, markup and CSS. This section has now been restacked, re-columned, restacked and A/B tested - do not rearrange it again without being asked.

4. **Services** (`#services`) — now directly under the Accreditations strip (it arrived out of the About hold until the 8 Sep 2026 reorder), with **a small entrance**: label, heading, then the six cards settle upward, staggered 0.16s to 0.46s by `nth-child`. Only `opacity`/`transform` are animated, so six cards at once cost no layout work. `IntersectionObserver`, fires once and disconnects - an entrance, not a state that replays on every scroll-back.
   - **The hidden state is added by the script (`.reveal-ready`), never in the markup**, so with JS off, no observer, or reduced motion the section is simply visible - the same rule the narrative fill follows.
   - **`.reveal-ready` is removed 1300ms after the reveal.** It carries the transition *and* the per-card delays, and leaving it on would hand any future hover or focus transition on those cards an inherited delay of up to 0.46s. Nothing on `.service-card` transitions today, so this is a trap being closed rather than a bug being fixed.
   - the 6 real DigiBlu services, sourced from the live site. Each card has a **Learn more** button (`.service-learn-more`, `data-service` key) opening a shared detail popup (`#serviceModal`) with a fuller breakdown per service — content adapted from each service's dedicated digiblu.com page (e.g. `/ai-artificial-intelligence`), stored in the `SERVICES` JS object near the bottom of `index.html`. The popup's footer CTA ("Discuss this service") closes the service modal and hands off to the contact modal on the same click — see the JS comment above `if (contactBtn) contactBtn.addEventListener('click', closeModal);` for why that IIFE must stay ordered *before* the contact-modal IIFE in the script.
   - **Corners are 10px, not 18px** (8 Sep 2026): feedback was the cards read as too curved, but a square card was not wanted either.
   - **Every card keeps its hairline border.** Removing the outlines was asked for on 8 Sep 2026 and reversed within the hour - without it the `.plain` card is distinguished from the page by its `--surface` tone alone, which in dark theme is #050505 against #000000.
   - **The A/B is over: the alternative section (`#services-alt`, white cards with an accent bar, art bands cropped from the hero and case-study artwork, a + disc) was deleted on 9 Sep 2026** - markup, CSS, the `--svc-alt-line` token, its breakpoint lines and its hook in the service-modal opener selector. It was built to a supplied reference on 8 Sep, reworked through that day (blue/black cards, real artwork bands with six cool hues, a white-card rebuild, Title Case, shorter cards, the hairline on the copy only), deleted once in favour of the grid, reinstated for other stakeholders, and then dropped for good. All of it is in git history (`ba336d9` is the last commit with it live). `hero.svg` and `cases-bg.svg` are no longer cropped by anything else; the rebuild script needed no change since its `String.Replace` calls simply find one occurrence each now.
   - **Card 6 is "Managed Services"** (renamed from "Post-Implementation" on 8 Sep 2026, on feedback) in the card, the footer Services column, the generated pages' footer and the `SERVICES.post` popup title; the `post` key was left alone. Its first popup bullet was retitled "Managed support" so the title is not repeated inside its own list.
   - **All six cards are `.accent` (blue) since 8 Sep 2026**, on request ("make it all boxes blue"); the `.plain` class and rule are gone. **The accent is the team card's gradient since 9 Sep 2026** (`#1b4fd6` to `#0a1f6e` to `#050b28`, the same value `.team-slice.active` uses) - asked to sit darker, like Meet the Team; it launched on the lighter `#4a8dff`-to-navy sweep that `.blog-art.a1` still carries. The paragraph below is the history of why the accent set was 1, 3, 5 and is kept for the content-swap it records.
   - **`.service-card.accent` sat on positions 1, 3, 5 (Artificial Intelligence, Process Excellence, Target Operating Model)**, alternating with `.plain` on 2, 4, 6 — on request, after the original 2/3/5/6 accent grouping put two blue cards back-to-back in the single-column mobile stack (positions 2+3, then 5+6), which read as an odd, clumped rhythm once the 3-column desktop grid collapsed to a list. Fixing it meant swapping the *content* of cards 2 and 3 (Process Excellence moved to position 3, Opportunity Discovery to position 2) as well as their accent/plain classes, since the desired accent set (AI, Process Excellence, Target Operating Model) didn't line up with the original position order. `data-service` moved with each card's content, so the Learn More wiring and `SERVICES` lookup needed no changes — only the markup order and `service-num` labelling were touched.
   - **No `.spark` icon any more.** Each card used to carry a small decorative 4-pointed sparkle SVG (`.service-card .spark`, absolutely positioned bottom-right) — removed on request, along with its now-dead CSS rule. Nothing else in the card depended on it (`position: relative` on `.service-card` was never solely for its sake), so this was a clean removal with no layout side effects.
5. **Case Studies** (`#case-studies`) — full-bleed `assets/cases-bg.svg` background (a vector rebuild of `footer-bg.jpg`, same method and verification as the hero - see the Files table; the artwork mirrors the hero's, frames left instead of right). Uses a `.cases` (padding + background, full width) / `.cases-inner` (max-width, no vertical padding) split — **keep it that way**; padding on the inner wrapper only was the rhythm bug mentioned above. **All content is real**, sourced from digiblu.com/case-studies: 3 featured engagements on the page (**SSE/OVO, AssuranceSD and Cedar Creek Church since 8 Sep 2026, on request** - Old Mutual and the healthcare workforce provider were featured before; all eight remain in the reader), each with a **Read more** opening the case-study **reader** (`#caseDetailModal`), plus a **View all case studies (8)** button opening the same reader. All 8 write-ups live in the `CASE_STUDIES` array in the script. The reading pane shows headline stats, Overview / The problem / What we did / Outcome, and the published client quote. The popups still open in-page with no navigation for a normal click — **Read more** buttons and sidebar rows are real `<a href="case-studies/<key>.html">` now, each also has a standalone page (see Static pages above), so only modifier-clicks/crawlers ever actually navigate there.
   - **The sidebar paginates at 10 rows.** `PAGE_SIZE` in the case-study IIFE; `#caseReaderPager` renders prev / numbered pages / next below the list, and **only when there is more than one page** - a lone "Page 1" is noise, so at today's eight case studies nothing shows and it turns itself on at the eleventh with no further wiring. The page follows the selection: opening a case study from a card calls `markActive`, which switches to the page holding that entry before highlighting, so the reader never highlights a row that is not on screen. **`.reader-list`'s horizontal padding is load-bearing, not spacing**: rows carry `margin: 0 -14px` so the selected/hover fill bleeds past the text column, and a scroll container clips what overflows it - so the moment the list became the scroller, that bleed was cut and the selected blue row rendered visibly cropped down its left edge (reported). Matching `padding: 0 14px` with a compensating negative margin gives the bleed room inside the scroller. **The list scrolls, not the aside** (`.reader-list` took over `overflow-y`, `.reader-side` is now `overflow: hidden`) - otherwise the pager sits below ten rows of content and you have to scroll to reach it. Buttons go to 38px below 900px for touch. Verified by temporarily setting `PAGE_SIZE = 3` against the real eight entries, which is the only way to exercise the multi-page path before an eleventh case study exists.
   - **On a phone the close X is a header row, not a float.** At or below 640px the panel is a column and the reading pane is its own scroller, so the absolutely positioned X (a child of the panel, not the pane) stayed put while the photograph, eyebrow pill and stat boxes scrolled under it (reported 2 Sep 2026). It is now in flow above the pane, right-aligned, the panel is `overflow: hidden` and the pane scrolls beneath it - the contact modal's pattern, including the `min-height: 0` without which the pane grows the panel instead of scrolling. Applied to the service dialog too, which shares the structure. Above 640px the pane's 52px gutter keeps the X clear, so desktop is untouched.
   - **Reader layout, not index + detail.** On request ("all articles should be left side of this screen and read full article on the right, apply same logic throughout i.e. case studies") the separate index popup (`#caseIndexModal`) was removed and the detail dialog became a two-pane reader: `.reader-side` (330px, every engagement as a `.case-index-item` row, the one being read on brand blue via `.is-active` + `aria-current`) beside the existing `.case-modal-body`. `.modal-panel` was already a flex row, so the two columns each scroll independently inside its max-height; `.modal-panel.reader-panel` raises the max-width to 1140px (doubled selector because `.case-modal-panel`/`.service-panel` set their own at equal specificity, later in the file). Picking a row calls `select(key)` - populate in place, reset the reading pane's scroll, and nudge the sidebar only if the row is out of view, so clicking a row near the bottom does not yank the list to the top. **Below 900px** the sidebar is `display: none` and the footer's "All case studies" button (`.case-back-btn.reader-toggle`, hidden on desktop) adds `.show-list` to the panel, which swaps the reading pane out for the list; picking a row swaps back. "View all" opens the reader on the last-read entry (first, before any) with `.show-list` set, so on mobile it lands on the list. Verified at 1829px and 375px in both directions, plus ctrl-click still navigating.
   - **A tenth case study, "Building a strategic partnership for AI transformation" for Safari Destinations, was added on 10 Sep 2026** (key `safari-destinations`, sector Travel & Tourism, service Artificial Intelligence) from copy DigiBlu supplied, **taking AssuranceSD's featured slot** (featured 2; AssuranceSD stays in the reader at order 3) and **second in the reader** (order 2), both on request. Same reformatting as the ninth: one paragraph per section condensed from the supplied multi-paragraph Background / Problem / Solution / Outcome, the title recast as a gerund ("A Strategic Partnership for AI Transformation" in the brief), the three "big stats" phrases split into pill value + label, three card tags. **The quote is an explicit placeholder** - text "[Client quote to follow]", cite "[Client name], [Client role] - Safari Destinations" - because the brief marks it `[INSERT CLIENT QUOTE]`; it renders in the reader and on the page as written until DigiBlu supplies it. The client is named (it is in the client logo strip too). **Photograph: Pexels 36048580**, an acacia tree on the Serengeti plains under a blue sky, viewed on its Pexels page for signage before it was proposed, chosen by DigiBlu over a herd of elephants (9185426), fetched through the same `?w=1600&h=600&fit=crop` parameters as the rest (146KB).
   - **A ninth case study, "Automating supplier quote processing with Generative AI", was added on 10 Sep 2026** (key `quote-processing`, client "Mobility equipment manufacturer", sector Healthcare, service Artificial Intelligence - **an earlier label lasted an hour and was changed because its wording hinted at the client's name; the user asked for "mobility manufacturer or something like that". Healthcare rather than Manufacturing as the sector so the eyebrow does not read "manufacturer · manufacturing"**) from copy DigiBlu supplied, **and it took Cedar Creek Church's featured slot** (featured 3; Cedar Creek stays in the reader at order 4, the new one at order 3). The client is **anonymised at DigiBlu's request** the way the healthcare workforce provider and the US city agency are - the key and the URL carry no client name, and **nothing in this repository names it, because the repository is public** (the real name was scrubbed from these notes on 21 Sep 2026; do not add it back, here or in a commit message). Reformatted to the set's shape: one paragraph per section, condensed from the supplied multi-paragraph sections keeping DigiBlu's facts and phrasing (the Cedar Creek precedent), the supplied title "AI-Powered Quote Processing Automation" recast as a gerund sentence-case headline like the other eight, the three supplied "big stats" (phrases, not numbers) split into pill value + label ("AI-powered / Quote processing", "End-to-end / Quote receipt to resolution", "Human-in-the-loop / Exception management"), and three card tags on the home page. **The quote is the "e.g." placeholder from the brief**, attributed as supplied to Tim Sunley, Client Engagement Lead, DigiBlu, and marked `[INSERT QUOTE]` in the brief - it stays until DigiBlu confirms or replaces it. **Photograph: Pexels 36006588** (the front of a large modern warehouse with closed doors, grey cladding, no lettering), chosen by DigiBlu on 10 Sep 2026 after asking for "a manufacturing unit or building", fetched through the same `?w=1600&h=600&fit=crop` parameters as the other eight (66KB) straight into `public/assets/case-studies/`. **The first choice, Pexels 4061583, was downloaded and dropped the same hour**: its alt text said "low rise industrial building with blue wall" and the building turned out to carry a real fabricator's sign across its front - the alt text is not a branding check, so **view a candidate before proposing it** (the Pexels photo page in the Browser pane shows it without downloading anything). The share card exists (`public/assets/og/case-studies/quote-processing.jpg`, built as the others). The overview keeps the brief's own words, "a large national manufacturer and provider of mobility solutions".
   - **First National Bank was replaced by Cedar Creek Church on 8 Sep 2026** (key `cedar-creek`, Faith & Community, Digital Solutions: six Jotform workflows in three weeks), on request, from copy DigiBlu supplied. It went in verbatim first (four multi-paragraph sections) and was **condensed the same day on request** ("seems long in comparison") to one paragraph per section like the other seven, keeping DigiBlu's own facts and phrasing; the full text is in git history. The quote is a contiguous excerpt - the first three sentences of the supplied quote - not a paraphrase. `addSection()` and the generator still split a body on a literal `\n` into one `<p>` each, added for the verbatim version; nothing uses it today, but a future multi-paragraph entry can. The FNB static page, photo and share card were deleted; the sidebar still counts eight.
   - **The stat pills have no ring since 10 Sep 2026** ("remove the outline, apply throughout the case studies section"): the faint white 1px border, a rim for the flat-blue version, read as an outline on the sweep. `.case-stat` is its fill alone, in the reader and on every case-study page. The home page's `.case-tag` hairline chips and the "View all" ghost button keep their borders - they are outlines by design, not rims - unless asked.
   - **The client quote's left rule is flat brand blue (`--blue`) in both themes since 10 Sep 2026**: it carried the `--brand-ink` sweep, which in dark theme is the family's bright end (#6aa4ff to #1d6ef5) and was reported as needing to be "brand blue" beside the navy pills. A 3px rule cannot show a gradient, and the fill's navy end is 1.4:1 on black, so the flat blue is the honest match for what the pills read as.
   - **The headline stats are brand-blue pills** (8 Sep 2026, two requests the same day: first hairline pills matching the section-header `.pill` in place of the solid boxes, then "make the pills brand blue, keep the shape"): `.case-stat` is a 999px pill filled `var(--blue)` with a faint white ring, the number bold beside a small uppercase label on one line, in the reader and on every static page since both share the CSS. A fixed-colour surface in both themes, so ink is pinned white locally and **both texts are full white** - white on #1d6ef5 is 4.56:1, so the muted alpha the hairline version used would land under 3:1 and fail AA at 11px. Pills wrap; there is no mobile full-width rule.
   - **`.case-headline` carries a 17em measure so all three featured titles wrap to two lines.** The column is ~22em and the titles run 24.5em, 22.3em and 20.8em unwrapped, so the shortest (Cedar Creek) sat on one line while the other two wrapped (reported 8 Sep 2026). Any value between 12.25em (half the longest, or it takes a third line) and 20.8em (the shortest, or it does not wrap) works; 17em sits mid-window so a font fallback cannot tip it. In `em`, so it tracks the clamped font-size at every viewport - verified two lines from 375px to 1664px. Re-measure if a featured title changes length materially.
   - **Each case study leads with a real photograph** of its sector, **and nothing else**. **In natural colour under a light neutral vignette since 8 Sep 2026** - the brand-blue scrim and `grayscale(0.55)` described below were removed on request ("get rid of blue tint on the images"); the notes below on the scrim unifying the set are kept as the reason it existed. **16/6 on desktop, 16/4.5 at or below 640px** - at 16/6 the photograph took a third of a phone's first screen before the title appeared (reported 2 Sep 2026). `.case-art-photo` is an `<img>` at `inset: 0` with `.case-art-scrim`, a gradient sibling, above it. The photo is `grayscale(0.55)` under a blue scrim, which is what lets eight photographs from eight sources read as one set - it also lands a warm source (the sunset pylon) and a cold one (the green hospital corridor) in the same key instead of fighting. **The `.aN` gradient underneath is kept on purpose**: it is what shows if a photo fails to load, so the panel degrades to exactly what it used to be.
     - **Paths are literal, in a `CASE_PHOTO` map, and that is load-bearing.** The obvious version builds the src from the key (`'assets/case-studies/' + c.key + '.jpg'`), which works perfectly locally and breaks the artifact: the rebuild inlines assets by **replacing the path string**, and a concatenated path appears nowhere in the file for it to find, so every photo would 404 in the published artifact. The rebuild loop throws if a path is missing rather than silently shipping a broken build.
     - The same markup is emitted by `generate-static-pages.js` with a `../` prefix. Verified in both: the modal (`naturalWidth` 1600) and a static page.
   - **The art panel sits above the title** in both the detail modal and the 8 static pages, reusing the blog art classes (`.blog-modal-art` / `.blog-art.aN`) deliberately, so the two are the same object rather than two that must be kept looking alike - the same accepted naming debt as the blog reusing `.case-viewall-btn`. The gradient is picked by position in the array so neighbours never share a panel.
   - **`CASE_ART` is gone.** The panel carried a service-line icon, then briefly a unique per-engagement icon (a stopwatch for halving onboarding time, a mortarboard for a training programme, a hub for a Centre of Excellence), and both were **removed on request** once the photographs arrived - an icon on top of a photograph is just something sitting on the picture. The object, its extraction in `generate-static-pages.js`, and the `.blog-art svg { position: relative }` rule that lifted the icon above the scrim all went with it. The icon set is in git history if it is ever wanted back. The `.blog-art svg` sizing rules were deleted with them in the 2 Sep 2026 code review.
   - **`.pill` gets a background here**, unlike every other section's plain text-only pill (`.cases .pill { background: rgba(0,0,0,0.35); border-color: rgba(255,255,255,0.4); }`). This looked like a non-issue at first — `getComputedStyle` showed the same border width/radius/color as every other section's pill — but the *photo* underneath it isn't flat like other sections' backgrounds, so the effective border contrast against the actual composited pixels measured only 2.35:1 (checked by drawing `footer-bg.jpg` to a canvas, applying the same gradient scrim math the CSS uses, and sampling real pixels under the border), well under the clean separation every other pill gets for free. The dark chip restores that separation without changing the pill's shape, radius or text treatment.
6. **Clients** (`.clients`) — **14** real client logos in an **auto-scrolling carousel**, reproduced from digiblu.com's "Our Clients represent multiple sectors & geographies" row. Rendered as **single-ink masks**, not the original colour art (see the Files table) — `background-color: var(--text)` means one rule handles both themes with no per-logo overrides. Muted at rest, full on hover.
   - **It is a carousel now, not a static row.** The earlier note here said a second marquee was deliberately avoided; that was reversed on request, because digiblu.com's own client row *is* a carousel and holds 14 logos rather than the 5 that were on the page. Two equal-width `.clients-group`s animate to `translateX(-50%)`; only the first is in the accessibility tree. **Loop is 27s on desktop, 21s at or below 620px**, paced to match the Accreditations strip on request (78s -> 40s -> 27s over two rounds of feedback). **Speed is the thing to hold constant, not duration**: the rows are different widths, so equal durations read as different speeds. Accreditations covers 1623px in 20s = 81px/s; Clients covers 2232px (desktop) and 1735px (mobile), needing 27s and 21s to land at 82.7 and 82.6px/s - all three within 2%. **Re-derive these whenever logos are added or removed**, since the group width moves with them and a stale duration silently changes the pace.
   - Sizes are **area-normalised, not height-matched**: the lockups run from icon+wordmark blocks to thin single-line wordmarks, so equal heights would leave the wide ones dominant. Extreme-aspect wordmarks (AssuranceSD, GatenbySanderson) are stepped down further. `.client-logo` **must stay `display: block`** — width/height are ignored on an inline span and every logo collapses to 0×0.
   - The healthcare workforce provider's case study is **kept anonymous on request** - do not rename it, and do not record anywhere in this repository (it is public) which logo in this strip, if any, belongs to that client or to any other anonymised case study. The logos name clients; the case studies they relate to stay unlinked.
7. **Testimonials — removed.** The section (3 real published client quotes, a carousel, `.tst-*` CSS and the shared `initCarousel` helper) was deleted on request. The quotes themselves still live in `CASE_STUDIES` and render inside the case-study detail popups, so nothing was lost — only the standalone section. `initCarousel` had no other consumer and went with it.
8. **Team** (`#team`) — heading is "Meet the leadership team". Click-to-expand accordion of the **8 real DigiBlu board and exec members** (Vic Gysin CEO, David Williams CSO, Karen Potgieter CFO, Jon Hinder COO, Martin McCloskey Company Secretary, Dianne Harris, Dave van der Westhuizen, Nick Bantick) with their real published headshots — sourced from digiblu.com/about-digiblu, so this is attribution of published people, not the fabrication the content policy forbids. Replaced the earlier 6 generic practice-area role titles. Above 760px the open member is a **full-bleed photo with the name and role over it**, under the scrim. A two-column card - headshot left, bio right, hairline divider - was built here on request and then **reverted on request**, along with the bio that showed inside it. Removing that `min-width: 761px` block restored the overlay on its own, because every rule it overrode still stands in the base block. The bios still live in `TEAM_MEMBERS` and still show in the profile dialog, which is now the only place they appear. The + affordance is a **bare plus in the blue disc** - it used to draw its own ring inside that disc, two concentric circles with the ring only 43% of the disc, which read as the plus sitting badly in the circle; it measured perfectly centred both before and after, so the complaint was proportion rather than alignment. Strip height is **280px** (down from 440px, per feedback that the desktop boxes ran too tall). Collapses to a stacked list below 760px, where the photo becomes a 48px circle and the scrim is disabled - **mobile keeps the tap-to-open profile dialog unchanged, on request**, and never shows a bio inline.
   - **The desktop card is two columns again: photo left, name/role/bio right** (rebuilt 2 Sep 2026 on stakeholder feedback, after an earlier version of the same idea was reverted). The bio is a `<span class="team-slice-bio">` injected by the team IIFE from `TEAM_MEMBERS` - a span because the slice is a `<button>`, which allows phrasing content only - hidden by default and shown only inside the `min-width: 761px` card block. It is clamped to seven lines, which is the whole bio at card widths from ~1100px up; narrower desktops get an ellipsis and the + disc still opens the full profile. **The backdrop is the case-art blue-to-navy gradient, not flat blue** ("did not sit well" with the photo laid over it), still a fixed colour for the cutout-edge reason below, and the active photo shows in colour while the collapsed circles stay greyscale. The old bottom scrim is repurposed as the hairline between the columns. **The photo is a white circle, 168px (120px on narrow cards), using the collapsed slices' exact recipe - a centred 190% zoom on the face-centred crop** - so it lands on the face for the same reason theirs does. On request, after a square tile (first navy at cover, then white at 135%) still read as a picture cut out and placed in a box; the circle is the crop the strip already uses, so open and closed slices share one language rather than a raw column - painted straight onto the card, a cutout's flat bottom edge read as the picture being cut off; in a frame the same edge reads as the frame. **No + disc on desktop** (the bio is in the card), though clicking the open card still opens the full dialog, which matters below ~1100px where the bio clamps. Mobile is untouched: blue rows, tap opens the dialog.
   - **No bio in the strip.** *(superseded above - kept for the measurement history)* One was injected into each slice and shown above 1280px; that floor was measured rather than picked, since the strip is a fixed 280px and worst-case spare height ran 46px at 1280 but went negative by 1024. The card and the injection are both gone now. **If it is ever rebuilt, re-measure across all eight members** - Vic has the shortest bio, so checking only the default member clears a threshold the other seven overflow. **On that stacked list a tap moves the blue highlight as well as opening the profile** - it used to open the profile only, so the first slice kept its initial `.active` blue for the whole session and two rows read as selected once any other member was opened (reported bug). The mobile block pins white ink on `.team-slice.active` (role label at full white: white itself is only 4.56:1 on the blue, so the muted alpha the other rows use would fail AA).
   - **The selected slice sits on brand blue** (`var(--blue)`), not the theme surface. It has to be a fixed colour: the headshots are chroma-keyed cutouts, so their semi-transparent edges composite against whatever is behind them, and on `--surface` that is near-white in light theme and near-black in dark - the same cutout showed a pale halo in one and a dark fringe in the other. The scrim stays; labels measure 18.78:1 on the composited backdrop.
   - **The photo alpha has been repaired, and that repair is load-bearing.** Moving to blue exposed keying damage the old neutral surfaces had camouflaged: the luminance key had eaten into light clothing and left translucent patches and holes inside the subjects, which blue then showed through, and it had serrated the outlines. Fixed in the **images**: morphological close to seal channels the key cut through hair, hole-fill inside the resulting silhouette, nearest-opaque inpainting so filled pixels take neighbouring hair/skin rather than the backdrop underneath, then a 3x3 median on the alpha edge to remove serration (a 2px rim is preserved so edges stay anti-aliased). Note the photos render under `filter: grayscale(1)`, so any blue *in* the image would appear grey - visible blue can only ever be the background showing through. Verify by compositing over the real blue at the real crop **with the scrim**; without the scrim the lower clothing looks far worse than it renders.
   - **The photos must stay face-centred, and `background-position` must stay `center center`.** The active slice is a wide, short box (783×280 on desktop), so `background-size: cover` on a square portrait shows only ~36% of the image height — a horizontal band. With the subject merely centred in the square, that band landed on the torso/hands rather than the face, which is exactly what a reviewer reported. Nudging `background-position` by percentage cannot fix it, because the active slice's aspect ratio changes with viewport width, so any fixed percentage is only ever right at one width. The fix is in the **image**, not the CSS: `scratchpad/process_team_faces.js` finds the head from the alpha silhouette (the first row whose opaque width reaches ~62% of the subject's maximum width is the neck/shoulder line, since a head is markedly narrower than shoulders), then crops a square centred on the eye line. With the face at the image centre, a centred cover-crop frames the face at *every* aspect ratio. Re-run that script rather than reintroducing a positional offset.
9. **Blogs — removed.** The whole blog was deleted on request: the homepage section, the `#blogDetailModal` reader, the `BLOG_POSTS` data, both blog IIFEs, the 7 generated `blog/*.html` pages and their sitemap entries, plus every nav, mobile-menu and footer link to it. **The `.blog-art` / `.blog-modal-art` CSS survives on purpose** - the case studies reuse it for the panel above each engagement title (see Case Studies above), so it is now art-panel CSS that merely carries a blog name. Everything else blog-specific was removed from `assets/site.css` after checking each selector against the remaining markup: `.blog h2` came out of the shared section-heading list, and `.blog-card`/`.blog-filter` out of the shared `:focus-visible` list.
   - **Capped grid + a reader sidebar.** Every filter (including "All") shows only one row of matches - the cap reads the live column count, so the row stays full at 3/2/1 columns rather than leaving an orphan card. The rest of the library lives in the reader dialog's sidebar (`#blogDetailModal .reader-side`, same `.reader-*` rules and behaviour as the case-study reader above), not an inline expansion: "View more" used to reveal the rest in place, which grew the page without bound as posts were added, and a separate index popup (`#blogIndexModal`, since removed) replaced that before the reader did. **The sidebar lists every post regardless of the active topic filter** - it is the whole library, and filtering it would leave a post that a card just opened missing from its own list - so the "View all" count is `items.length`, not the filter's total. `window.__blogFilter` went with the popup. Rows are real `<a href="blog/<key>.html">`, so crawlers and modifier-clicks get the static page. The button reuses `.case-viewall-btn`/`.case-count`/`.cases-actions` - known naming mismatch, not a bug.
   - **Cards open a popup**, not a link-out. Clicking a card opens `#blogDetailModal` with condensed key-point takeaways by default (`BLOG_POSTS[key].points`), plus a "Read full article" button that expands **in place** to the complete verbatim article (`BLOG_POSTS[key].full`) — on request ("Blog articles should read like full articles not summaries... defaulted by summary, then click the button and it expands"). The modal reuses `.service-panel` / `.service-modal-body` / `.service-modal-eyebrow` / `.service-modal-list` / `.service-modal-item` from the Services "Learn more" modal (same naming-debt tradeoff as above) plus `.blog-meta` from the cards themselves for the date/read-time row. Cards are `<a href="blog/<key>.html">` (each post also has a real, standalone page — see Static pages above), styled to look like plain flow content via a CSS reset (`background: none; border: none; padding: 0; text-align: left; font-family: 'DM Sans', sans-serif; text-decoration: none;`); a plain click still opens the popup, ctrl/cmd/middle-click or a crawler gets the real page.
10. **Footer** — `background: var(--bg)` (black in dark theme, white in light, i.e. identical to the rest of the page) with a single `border-top: 1px solid var(--hairline)`.
   - **Do not give the footer its own background tone.** Three treatments were tried and each was rejected in turn: a full-bleed artwork band (much earlier), a **brand-navy/blue** band ("not a big fan of the blue footer"), and a **deeper-neutral** `--footer-bg` band. The settled position is that the footer should "match and marry up with the background of the wider website", and that **the dividing line is the only separation worth keeping** — so the hairline is load-bearing and the background deliberately is not.
   - The `--footer-bg` token introduced for the neutral-band attempt has been removed; don't reintroduce a footer-specific background variable.
   - Measured after the change: footer background is byte-identical to the page background in both themes, the 1px rule reads at 2.27:1 (dark) / 1.89:1 (light) against it, and footer text sits at 6.13–6.27:1 worst case. Text is theme-aware — the footer no longer pins its own ink, so the logo mask (which paints with `currentColor`) follows the theme on its own.
11. **Contact modal** - **2-step** form (details -> enquiry), dialog semantics (`role="dialog"`, focus trap, `inert` on background).
   - **History from here on where it describes the dialog: since 18 Sep 2026 the form is a page, `/contact`, and nowhere else** (`#contactPage`, a panel in the flow; `ContactDialog.tsx` and the overlay code are gone). Every Get in touch and Contact Us is a plain link to it, and the form's Privacy and Cookies Policy link opens the policy page in a new tab, so the in-place policy and dialog stacking described below no longer apply. The fields, the two steps, the consent gate and the phone-screen scrolling still hold.
   - **Only first name, last name and email are mandatory.** Company and message are optional and labelled as such; step 2 is "Your enquiry" (it read "Your business"). The **GDPR consent checkbox** (`#cf-consent`) stays required - it is a legal gate, not a data field - and is enforced by `currentStepIsValid()` (verified: blocked unchecked, succeeds checked).
   - **"Schedule a time" (the old step 3) was removed on request**, along with the `#cf-time` select and the IIFE that populated its 08:00-18:00 slots. The consent checkbox moved onto step 2, and it **has to live on whatever the last step is**: `currentStepIsValid()` only checks the step being left, so consent sitting on an earlier step would stop gating submission. `totalSteps` reads `.form-step` off the DOM, so the count itself was never hardcoded. The aside intro and the success message no longer promise to confirm a time.
   - **The `.modal-field select` rules are gone** (2 Sep 2026 code review). `#cf-time` was the only `<select>` on the site; the rules were kept for a while as "correct but unused" and then deleted as dead weight. The bullet below is kept as the *reason* a future select needs a per-theme chevron - the rule itself is in git history.
   - **The Privacy Policy link opens the policy in place**, via the same `data-legal` hook the footer links use, with the real `legal/privacy-policy.html` href kept for crawlers and modifier-clicks. It used to open that page in a new tab, which works on a deployed site but not in the published artifact, where the file does not exist - so there the link went nowhere.
   - **That required making dialog stacking safe.** `createModalController.close()` cleared `inert` and the scroll lock unconditionally, so closing the policy over the contact form handed the page back to the keyboard while the form was still open. It now checks for a still-open `.modal-overlay.open` and hands the lock to it instead. This is the focus-trap conflict the new-tab approach originally existed to dodge.
   - **`.modal-field select` needs a per-theme dropdown chevron.** The arrow is a data URI, so its stroke is baked in and cannot read `--ink-rgb` like everything else. It was hardcoded white: 5.93:1 on the dark field but **1.06:1 on the light one** - present in the markup, invisible on screen, which read as the select having no dropdown affordance. There is now one rule per theme (both light paths); the light chevron measures 3.88:1. The `background-size: 12px 8px` is also load-bearing - the SVG has a viewBox but no width/height, so without it the browser scales the chevron several times too large.
   - **Full screen on phones** (`#contactModal`, at or below 640px): overlay padding removed, panel at 100dvh with no radius or border. Reported as clunky to scroll, with fields seeming to be missing when they were not - which is exactly what the card did, being a short box whose whole panel scrolled as one, shrunk further by the on-screen keyboard until a field or two was all you could see with no sign there was more. **The panel also stops being the scroll container**: `.modal-main` scrolls instead, so the header and its step tracker stay put while the fields move under them, which is what tells you where you are. **`min-height: 0` on `.modal-main` is required, not tidiness** - a flex child defaults to `min-height: auto` and refuses to shrink below its content, so it would grow the panel instead of scrolling inside it. Scoped to the contact dialog; the other dialogs are still cards on mobile.
   - **Posts to `/api/contact` since 11 Sep 2026** (see Pages over dialogs and the contact API in `consent-and-contact.md`). ~~The developer's email call inside it is the outstanding blocker in Known follow-ups.~~ **Done 14 Sep 2026**: Radu's Azure Communication Services call in `lib/contact/send.ts` emails each enquiry to DigiBlu, and since 18 Sep 2026 a successful send fires a GA4 `generate_lead` event when analytics is allowed.

12. **Scroll-to-top button** - bottom-right, appears past 480px scroll, respects `prefers-reduced-motion`. On every standalone page too since 21 Sep 2026 (below), and hidden while the cookie banner is up, since both sit bottom right.

### Standalone pages' chrome (21 Sep 2026)

Asked for on the case-study, case-study index and services pages, applied to every standalone page: the nav is the home page's fixed, frosted bar and the scroll-to-top disc is there too. `PageHeader`'s `nav.page-header` sits inside `div.site-nav.page-nav#siteNav`, so `NavChrome` measures `--nav-h` from it and toggles `.scrolled` past 24px exactly as on the home page, and `.page-header .logo/.theme-toggle/.nav-toggle` keep their theme ink at rest (there is no hero behind them). `.page-nav + main { padding-top: var(--nav-h) }` replaces the height the nav took in flow: measured before and after, the first line sits at 147px on desktop and 131px on a phone either way. Each page renders `<ScrollTop />` after its footer; `NavChrome`'s scroll-top block already wired `#scrollTopBtn` wherever it exists. Checked in `digiblu-vinext`: glass at 12px when scrolled (both themes), the mobile menu opens under the bar, `/services#discovery` lands 59px below the nav on a phone, no horizontal overflow.

### Dialogs (history)

> **There are no dialogs since 18 Sep 2026** (the `pages-only` branch, merged into `dev` on 21 Sep 2026, at Radu's request). The six below, `createModalController`, `lib/client/modal.js` and `components/dialogs/` are all gone; every piece of content and the contact form is a page of its own, and `pnpm test:pages` fails if a dialog, the form or document content reaches the home page again. The only dialog on the site is vanilla-cookieconsent's cookie preferences dialog, which brings its own focus handling. This section is kept for the reasoning; do not rebuild content dialogs from it.

There were six `.modal-overlay` dialogs: `#contactModal`, `#serviceModal`, `#caseDetailModal`, `#badgeModal`, `#legalModal`, `#teamModal`. (`#blogDetailModal` went with the blog.) The two index popups (`#caseIndexModal`, `#blogIndexModal`) were folded into the detail dialogs as reader sidebars - see Case Studies and Blogs above.

**Dialogs could stack.** The contact form's Privacy Policy link opened `#legalModal` over it, so `close()` in `createModalController` checked for a still-open overlay and handed `inert` plus the scroll lock to it rather than clearing them - releasing them unconditionally handed the page back to the keyboard while a dialog was still up. The two case dialogs shared a `createModalController(overlay)` helper that handled background `inert`, the focus trap, Escape and backdrop-click. ~~**New dialogs should use that helper**; the contact and service modals predate it and still carry their own near-identical copies, which is the obvious cleanup if anyone touches them.~~ (Moot: the helper and every dialog it served are gone.) `open(restoreFocusTo)` / `close(skipRestore)` take arguments specifically so the index → detail → back chain can hand the original opener across instead of restoring focus to a row that is no longer visible.
