# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Node (no dependencies are currently installed — http-server is run via
# `npx` on demand — but ignored defensively in case that changes)
node_modules/
npm-debug.log*

# Local scratch/output some tools may create
*.tmp
*.log

# Next.js / OpenNext
.next/
.open-next/
out/
next-env.d.ts
.wrangler/

# generated from content/**/*.md at build time (npm run content)
content/.generated/

# Environment files with real values (the example is committed)
.env
.env.local
.env.*.local
